# 智能分析决策平台——实施 TODO（5/6）

> 文档状态：唯一任务事实源（活文档，随实施更新）
> 基准日期：2026-08-12
> 配套文档：[产品设计](./01_产品设计终稿.md)、[技术设计](./02_技术设计终稿.md)、[前端用户旅程](./03_前端用户旅程终稿.md)、[实施规划与验收](./04_实施规划与验收终稿.md)、[Handoff](./06_HANDOFF.md)
> 任务 ID 沿用历史编号；已完成任务基线见 [06_HANDOFF §3](./06_HANDOFF.md)，不在本文重复

---

## 0. 使用规则

1. 任务按 [04 §3](./04_实施规划与验收终稿.md) 的里程碑 M1～M6 组织；领任务先确认里程碑与依赖。
2. 状态按 BE / FE / E2E / BIZ 四层记账；勾选 `[x]` 表示该任务定义的全部交付完成，后端完成不得代表业务闭环。
3. 页面沿用已确认的蓝 / 白 / 灰设计系统与 1920×1080 桌面基准自主落地；只有会改变业务口径、权限边界或生产数据的决策才保留人工治理门禁，普通 UI/UX 延伸不再中断实施等待确认。
4. 新任务必须登记在本文件并声明六大支柱归属（见 [02 §3](./02_技术设计终稿.md)）；不得在本文件之外派发任务。
5. 数据库迁移编号已使用至 `000341`，新迁移顺延，不得复用；详见 [06_HANDOFF §4](./06_HANDOFF.md)。

### 0.1 UI/UX 全链重构执行队列

> 推进规则：按一个可完成的端到端主链连续设计、确认、实现和验收；只有主链当前步骤完成后才进入下一步。遇到拒绝、撤回、超时、角色差异或后续治理等分支时，登记到分支队列，不中断当前主链。

- [x] **UI-FLOW-P01-001** — 平台准入与权限主链（六大支柱：Workflow/Guardrail、Deterministic Systems）：`登录/注册 → 领域访问或申请 → 审批领域申请 → 配置成员与角色 → 切换领域 → 分析首页`。登录、领域访问、审批中心、用户权限、侧栏领域切换与分析首页均已完成新版页面；「审批通过 → 配置成员权限 → 切换领域 → 刷新分析首页」已连续回归，主链收口。拒绝、会签、对象级范围与停用保护继续按 B01～B04 分支队列推进。
- [x] **UI-FLOW-P02-001** — 数据源接入与资产化主链（六大支柱：Workflow/Guardrail、Deterministic Systems）：`数据源清单 → 新建连接 → 阶段化测试 → 提交发布审批 → 元数据发现/导入 → 业务元数据完善 → 进入数据集建模`。数据源清单、新建连接、测试与发布流程，以及资产目录、发现导入、后台任务进度、表/字段业务元数据完善、AI 建议复核、脱敏样本预览和数据集建模入口均已完成统一蓝白灰重构；资产化新增页面复用现有元数据发现、导入/刷新任务、表字段更新、AI 建议决策、样本预览与手工完成接口，主链页面收口。真实库与异常治理仍按 `E2E-DATA-001`、B01～B05 分支队列独立验收。
- [x] **UI-FLOW-P03-001** — 数据集建模与发布主链（六大支柱：Workflow/Guardrail、Deterministic Systems）：`数据集目录 → 选择已发布资产 → 字段/关联/粒度配置 → 预览与本地校验 → 保存草稿 → 提交发布审批 → 冻结发布版本 → 启动物化任务`。数据集目录、建模画布、草稿元信息、发布审批和物化运行均已完成统一蓝白灰 1920 桌面重构；生产页面继续沿用既有数据集、修订、候选预览、发布审批、版本与物化接口，主链已连续回归通过。保存冲突、审批驳回、运行异常、版本回滚与依赖保护继续按 B01～B05 分支队列推进。
- [x] **UI-FLOW-P04-001** — 核心语义建设与 Release 激活主链（六大支柱：Workflow/Guardrail、Deterministic Systems）：`已发布并物化的数据集 → 创建语义模型 → 实体/粒度/关系/时间合同 → 关键指标可加性与维度成员策略复核 → Owner 认证 → 固定认证对象生成 Release Candidate → 静态校验与四项投影 → 评测门禁 → 数据/业务 Owner 双人审批 → 原子激活 ACTIVE`。新增统一蓝白灰 1920 语义资产工作台、四步模型向导、模型/指标/维度/知识目录和发布中心；生产页面接入现有语义对象 CRUD、批量认证、可加性 Readiness、Release 生命周期与评测审批接口，并新增不暴露密封题面的 Release 目录、评测集元数据、已认证时间合同读取接口，以及由服务端解析权威合同与 Hash 的 Release 组合接口。主链已在同一页面连续回归通过；独立指标/维度/知识创作深链及异常治理继续按 WEB-007 与 B01～B05 队列推进。

分支待完成队列：

- [x] **UI-FLOW-P01-B01** — 领域申请驳回、撤回、重复申请合并与再次提交：本人可撤回待审申请；驳回/撤回历史保留审批意见并支持重新提交；并发重复提交通过待审唯一键合并为同一申请且更新用途，不再暴露唯一索引错误；页面已完成操作列与窄视口无溢出走查。
- [x] **UI-FLOW-P01-B02** — 敏感领域安全会签、双人审批与超时升级：领域敏感级别成为服务端唯一权威配置；敏感领域申请固定业务 Owner 与安全复核两个独立席位，同一账号不得双签，安全席位仅平台管理员可签。首签只追加不可变收据，不提前授予访问；双签完成后才原子激活成员关系。待审项按 24 小时 SLA 展示截止与三级升级，升级只改变紧急级别、不代替审批；管理中心已按席位展示当前签署与待签状态。
- [x] **UI-FLOW-P01-B03** — 对象级访问范围、最后 Owner 保护与权限影响预览：数据源和数据集的 PRIVATE/DOMAIN 共享范围由服务端按所属领域、资产持有人和管理员身份授权；收回领域共享前会计算将失去访问的成员、数据集/语义模型/报告依赖及他人持有对象，存在外部持有人依赖时服务端 409 阻断。无持有人资产不得设为 PRIVATE，避免形成仅管理员可达的孤儿对象；页面已在真实已发布数据集上验证影响清单和禁用确认按钮。
- [x] **UI-FLOW-P01-B04** — 用户停用、Owner 转交、孤儿资产防护与会话即时撤销：用户权限页停用操作已改为服务端影响预览 → 按资产类别/领域选择接收人 → 原子转交 → 自动关闭个人订阅/发送任务 → 撤销全部会话 → 停用账号；唯一领域/平台管理员和未完成审批会显式阻断，失败批次支持安全重试，不再绕过生命周期服务直接修改账号状态。
- [x] **UI-FLOW-P02-B01** — 修改连接配置、轮换凭据、测试收据过期与版本冲突恢复：编辑页明确“保留/将轮换凭据”且密码永不回显，新密钥只进入加密草稿、审批前不切线上版本；服务端版本冲突返回最新版本号，页面自动重新加载并要求复核；连接收据按 `testExpiresAt` 显示过期、阻止发布并提供重新测试，已完成浏览器状态与弹窗布局走查。
- [x] **UI-FLOW-P02-B02** — 发布申请驳回、撤回、重复提交与重新提交。
- [x] **UI-FLOW-P02-B03** — 元数据导入部分失败、失败表重试、任务取消与断线恢复。
- [x] **UI-FLOW-P02-B04** — 结构差异、破坏性变更影响分析与下游 STALE 处置。元数据差异已使用确定性兼容分类；删除字段、收窄类型/精度/可空性和约束变化会在同一事务中递归标记直接及传递发布下游为 `STALE`，资产页展示受影响数量、冻结状态和数据集修复入口，兼容性新增保持在线。
- [x] **UI-FLOW-P02-B05** — 数据源安全下架、迁移/终止方案、ACTIVE 下游阻断与凭据撤销。下架前置接口会列出直接来源与版本依赖数据集，管理页提供逐项迁移入口并在阻断消除前禁止删除；最终事务再次锁定检查依赖，关闭连接后撤销数据源及全部历史连接版本凭据、停用元数据资产并保留审计证据。真实 MySQL 验收进一步发现历史版本的通用不可变触发器会拒绝凭据墓碑、导致对象卡在 `DELETING`；迁移 `000338` 只放行退休状态下、只改 `secret_ref` 且目标值精确绑定源 ID 的不可逆撤销，其余版本字段继续不可变，并自动修复此前已删除对象。
- [x] **UI-FLOW-P03-B01** — 保存时 409 版本冲突、草稿被他人更新与重新加载恢复。草稿与元信息保存冲突会返回服务端当前版本和 DSL 摘要；设计器保留本地未保存配置，展示明确协作冲突和“加载最新草稿”恢复入口，避免静默覆盖。
- [x] **UI-FLOW-P03-B02** — 发布申请驳回、撤回、草稿变更自动取消与重新提交。审批申请支持申请人撤回并记录审计，拒绝继续强制填写意见；草稿变化自动取消精确旧申请，撤回/驳回/自动取消后允许同一草稿重新提交，同时只保留一个待审申请，并发状态变化会自动刷新。
- [x] **UI-FLOW-P03-B03** — 物化任务部分成功、失败、取消、重试与刷新 SLA 告警：物化控制面统一返回服务端计算的刷新截止时间、临期/超时状态与耗时；运行详情汇总成功、失败和待处理节点并明确部分成功；数据集目录直接呈现失败码与 SLA 告警，支持停止活动运行、诊断失败节点和基于当前发布版本创建新的可审计重试。质量失败、部分成功或取消均不切换 ACTIVE 快照。
- [x] **UI-FLOW-P03-B04** — 发布版本差异、影响预览、回滚为新草稿与重新发布：历史版本页按字段、DAG 节点和业务元信息计算与当前草稿的确定性差异，标出潜在破坏性变化，并通过精确版本 usage 接口汇总草稿、已发布下游与运行中查询影响；回滚继续复制源修订生成新草稿，历史发布快照和当前发布指针不被覆盖，保存后可重新提交审批。
- [x] **UI-FLOW-P03-B05** — 数据集停用、恢复、删除与下游依赖保护：新增生命周期影响预检接口，统一返回下游草稿/发布引用、活动查询、活动物化和待清理物化数量；目录直接提供停用、恢复和删除操作。停用保留停用前稳定状态并可原子恢复；删除在影响未清零时阻断，成功后只软删除控制面并异步清理数仓物化，绝不修改业务源表。
- [x] **UI-FLOW-P04-B01** — 语义草稿 409 冲突、认证驳回、依赖缺失与重新加载恢复：语义模型“继续完善”会加载原草稿及其真实数据集、粒度和时间合同，更新携带 `expectedUpdatedAt` 乐观锁；409 时保留本地输入并提供加载服务端最新草稿，认证校验失败展示稳定错误码与恢复说明；数据集版本、物化收据、字段、粒度键或时间合同缺失均在向导内阻断并给出对应建设入口。
- [x] **UI-FLOW-P04-B02** — 可加性、时间合同、成员索引、Fanout 与敏感策略冲突的批量修复：指标按各自服务端建议值分组确认可加性，不再发送占位枚举；敏感维度安全收敛为 `EXACT_ONLY`，未声明安全策略的一对多/多对多关系默认 `BLOCK`，不自动猜测业务桥接；时间合同继续在模型向导中逐项绑定已认证合同，缺失时可创建领域默认合同并重新校验。
- [x] **UI-FLOW-P04-B03** — Release 预检失败、局部投影失败、Hash 不一致、评测失败与重新执行：预检 422 现在返回对象级稳定问题码；生命周期接口返回四类投影状态、期望/实际 Hash、尝试次数与失败码；发布中心诊断弹窗按预检、投影、评测三层展示阻断，只重试失败投影，Hash 漂移要求新建候选，门禁失败可在密封评测集修复后重新运行，1920 基准视觉回归通过。
- [x] **UI-FLOW-P04-B04** — 双人审批驳回、同人双签阻断、超时升级、撤回和再次提交：新增追加式审批撤回、驳回轮次重置与三级超时升级账本；有效审批查询排除已撤回收据，同一有效轮次继续强制两种职责、两个不同账号；页面同时支持批准/驳回、本人撤回、修复后重置并重新签署，门禁通过后按 24 小时 SLA 展示截止时间与升级级别，全部原因只保存不可逆 Hash，原始审批历史保持不可篡改。
- [x] **UI-FLOW-P04-B05** — Shadow/Canary、暂停、回滚、Release 引用保留与安全退役：READY 候选先进入真实隐藏双跑，后台复制受控问题信封并以候选 Release 执行，不创建会话、报告写回、收藏、反馈或数据申请等用户可见副作用；一致性以终态和用户可见业务结果稳定指纹判定，不再误比包含发布/计划身份的技术 Hash。随后按稳定用户分桶推进 5%/20%/50%/95% Canary，服务端直接汇总回答、澄清、阻断、安全、P95 时延与成本并自动止损；暂停/恢复保留原分桶盐，受控回滚原子恢复旧 Release。历史引用通过 RETAINED 与 24 个月保留窗保护，引用未解除或保留期未到不得退役。发布中心已提供同一运行治理弹窗并在 1920×1080 完成无遮挡、无横向溢出和真实数据集入口回归。

## 1. 共用引擎（M4 前置，最高优先级）

- [x] **AI-005** — 报告与问数统一 LLM 引擎阶段协议：已冻结「识别—分析—规划—指派」的阶段合同、事件流与审计字段；两条链路共用，不展示隐藏思维链。
      **已产出待确认提案：[07_AI-005_阶段协议提案](./07_AI-005_阶段协议提案.md)**，把既有的三张约束表
      （`valid_question_run_transition` 状态图、`allowedToolsForCognitionStage` 阶段→工具、
      `stageAllowsAction` 阶段→动作）连接为完整的阶段—状态映射，不新增权限也不放宽门禁。
      提案中有三个必须裁定的开放问题：`UNDERSTANDING` 阶段缺少「完成」动作、
      有界纠错次数上限、报告引擎是否共用同一张阶段表。
      现已由确定性理解、绑定、受控纠错、预算和置信门禁共同实现；报告改稿继续使用同一受控操作合同，不直接覆盖草稿。
- [x] **ASK-WORKER-001** — 问数运行 Worker 已接线：`cmd/worker` 每轮按租户领取运行，
      由 `tools.Assembler` 组装 `Binding → Registry → CognitionRunner → Loop`，
      按 AI-005 协议交替推进确定性状态与认知阶段，心跳续租并在结束时释放。
      `orchestrator.NewLoop`、`toolhost.NewRegistry`、`search.NewRetriever`、`graph.NewClient`
      至此均首次拥有生产调用方。
      已在真实 ACTIVE Release、数仓和运行 Worker 上跑通 `RECEIVED → ANSWERED`，并继续验证了历史恢复、负反馈、加入报告和形成决策。

## 2. M1 数据建模收口（P02/P03）

- [x] **E2E-DATA-001** — 真实数据源全链验收：使用 verification profile 中独立的 MySQL 8.4（非 PostgreSQL 方言）完成草稿→分阶段连接测试→审批→发布→两张表发现/导入→脱敏采样与元数据 AI 完善→映射数据集→源端增删字段→增量发现与 19 项结构差异→破坏性字段冻结下游→逐项清理依赖→安全下架。最终收据验证根凭据及全部历史配置凭据均变为不可解析的 source-specific tombstone，元数据表/字段全部停用，源库仍保持 5 行且金额汇总未改变。`make mysql-e2e-up|reset|shell|verify` 提供可重复环境；Oracle 仍属于外部生产环境签署，不影响本地产品全链可用。
- [x] **DWH-SLA-001** — 增量物化、检查点恢复、质量失败不切快照与刷新 SLA 告警的真实数仓验收（J-P03-06 异常支路）。增量运行在注册时冻结 ACTIVE 物化 ID、快照 Hash 和业务时间水位；Worker 只接受精确基线并将已完成节点作为检查点恢复，历史区未变时采用 `WATERMARK_APPEND`，检测到迟到/更正数据时安全回退 `HISTORY_CHANGED_FULL_FALLBACK`。真实数仓连续验证首次全量、增量切换、故意质量失败不切换 ACTIVE、超时告警和原位重试恢复，页面保留失败证据与恢复收据。

## 3. M2/M3 语义建模与发布（P04～P08）

- [x] **ADD-002** — 可加性启发式建议与补录清单：确定性启发式规则、Readiness、补录清单路由
      与批量确认此前均已存在，**真实缺口在建议的落库**：`additivity_suggestion` 只能由
      `cmd/askdata-inventory` 这条离线命令写入，而 `BulkConfirmAdditivity` 要求确认值与
      **已落库建议**一致——因此凡是未跑过该命令的指标，在页面批量确认时被静默跳过，
      永远无法成组确认。现新增服务端 `RefreshAdditivitySuggestions` 与
      `POST /metrics/additivity/suggestions`：候选与建议值全部由服务端推导，
      请求体为空，浏览器无法为任何指标提议可加性。指标中心新增补录面板，
      按建议值分组并以业务语言展示触发规则，规则无法判定的指标单独成组、不提供批量确认。
      建议始终只是建议，确认仍是 Owner 的显式行为（HUMAN-008）。
- [x] **SEM-CTX-001** — 业务补充信息采集与治理（机制部分完成；正式内容仍由 HUMAN-015 提供）。
      **范围已按现状收窄**：02 §5 列出的四类里有三类已经落地，不应重复建设。
      - 企业内部黑话/缩写词条：**已完成**。`askdata.business_terms` 已含词条类型、匹配模式、
        优先级、负向上下文、适用角色与有效期，且经 `SEM-DICT-WIRE-001` 接入查询期确定性精确命中。
      - 指标默认过滤：**已完成**。`metric_versions.default_filters_ast` 是受治理 Filter AST，
        且**确实在编译期生效**——语义编译器经 `astTranslator` translate 并强制布尔根节点，
        不是只存不用的字段。
      - 组织信息映射：**已完成**。`dimension_members.parent_member_version_id` 表达成员父子，
        `askdata.hierarchies`/`hierarchy_levels` 表达层级结构，组织树无需再建新对象族。
      - **本次补齐**：指标口径说明与维度值定义（迁移 `000340`）。
        新增 `metric_versions.business_definition`（≤4000）与 `dimension_members.definition`（≤2000），
        均为受治理版本化字段，进入内容 Hash 与 Release 契约，随导入模板采集
        （`businessDefinition` / `definition` 两列）并按普通草稿→认证流程治理。
        两者只作检索文档与 LLM 上下文证据，**不参与绑定**（02 §5）。
        **这解决了一个真实缺陷**：此前指标版本契约只有 ID 与 AST，
        `searchDocumentText` 因而产出空文本，指标**从来不是可独立检索的对象**——
        用口径措辞提问（如「含税还是不含税」）无法召回对应指标。
        口径同时经 `get_semantic_contracts` 的 `definition` 进入规划上下文，
        让模型能区分名字相近但口径不同的指标。
        敏感成员的定义与其标签同级保护：`RESTRICTED` 对象根本不产出检索文档，
        成员文档另受 `DecideMemberExposure` 失败关闭，定义不会随之泄漏。
      - **本次补齐**：业务级行级安全过滤策略（迁移 `000341`）。
        平台级行级安全早已存在（02 §8 三重裁剪与库内 RLS），但它无法表达
        「销售只看本区域行」这类**业务**规则——平台并不知道谁负责哪个区域。
        新增 `askdata.row_access_policies`：绑定到语义模型的布尔谓词，
        **复用既有语义 AST 与编译器翻译器**（与指标默认过滤同一条通路，
        同样的布尔根校验与参数绑定），只新增一个叶子节点 `SUBJECT_ATTRIBUTE`；
        阅读者的业务属性存于 `platform.subject_attributes`，由管理员授予、
        绝不自述，也绝不进入 Release。
        **失败方向是这项能力的全部意义**：阅读者对策略引用的属性没有授权时，
        该策略拒绝全部行——不是放行，也不是报错。放行会让控制形同虚设，
        报错则会让一次缺失的授权把模型对所有人下线。
        为此刻意**不提供**「未匹配即放行」选项：给出这个选项，它终将被选中。
        谓词必须至少引用一个 `SUBJECT_ATTRIBUTE`（否则只是所有人共享的默认过滤，
        却挂着访问控制的名字），也必须引用模型字段；
        属性键集合由服务端从谓词推导，不接受调用方声明，二者不可能不一致。
        策略在**写入、认证、Release 门禁**三处校验
        （新增 `RELEASE_ROW_ACCESS_POLICY_UNSAFE`），随 Release 冻结，
        并在编译期作用于**每一个**源节点——锚点模型与被 Join 的模型各自应用自己的策略，
        Join 不得成为绕过读取权限的通道；多条策略以 AND 组合，只能收窄、不能放宽。
        阅读者取值以绑定参数进入 SQL，从不拼接；
        `askdata.resolve_subject_attributes` 只解析会话本人的属性，已在真实库验证
        「A 无法解析 B 的范围」。因阅读者取值进入计划，两名阅读者的
        plan hash 本就不同，这正是防止跨阅读者复用结果的必要属性；
        resolution hash 仍不含阅读者身份。
        另提供 `row-access-coverage` 覆盖率接口：失败关闭是静默的，
        没有人被授予属性的已认证策略会让所有人查不到任何数据，覆盖率让这件事在
        发布前可见。
        **前端已闭环**（本文 §0.2：后端完成不代表业务闭环）：
        语义中心运营页新增行级访问面板——按「模型字段 × 比较方式 × 阅读者属性键」
        的结构化条件表单生成谓词（不让业务人员手写 AST），多条件以 AND 组合，
        并直接展示覆盖率与未覆盖成员告警；
        用户权限页新增「数据范围属性」面板，按人授予/撤销属性，
        并在文案中同时说明两个方向的后果（授予会放宽其可读行，撤销会使其在相关策略下读不到任何行）。
        属性键集合前端**不发送**，由服务端从谓词推导。
        **策略内容仍受 HUMAN-015 阻塞**：机制已就绪，具体「谁对应哪个范围」由业务方提供。
- [x] **SEM-READ-001** — 语义对象读取接口。**上一版描述有误**：按对象类型的列表/详情路由一直存在
      （`GET/POST /api/v1/askdata/semantic/{models|measures|metrics|metric-versions|dimensions|terms|kpi-bundles|relationships}`，
      由 `internal/askdata/http/admin.go` 循环拼接注册，因此不出现在字面量路由清单里）。
      真实缺口是这些读取接口**硬编码 `status='DRAFT'`**，导入并认证之后的 CERTIFIED 对象一律看不到。
      已改为：`ListObjects`/`GetObject` 支持状态过滤，不带 `status` 时返回全部状态，
      `?status=DRAFT|CERTIFIED|ACTIVE|DEPRECATED` 精确过滤，未知状态在存储层拒绝；
      写路径（Create/Update/Delete/可加性确认）仍固定 `DRAFT`，语义不变。
- [x] **SEM-READ-002** — 补齐读取接口：新增维度成员、层级、认证问法、指标维度绑定
      四类的列表/详情（`members`、`hierarchies`、`certified-examples`、`metric-dimensions`），
      沿用同一套状态过滤与游标分页；这四类目前只能经导入通道写入，因此**只注册 GET**，
      不注册未实现的写入路由。
      **评测用例（EVAL_CASE）刻意不开放读取接口**：`evaluation_case_versions` 含
      `set_type='SEALED'` 的密封集正文，而密封集正文不可显示、被查看样本必须立即退役
      （02 §10.1、03 J-P08-04、06 §4.10）。开放普通读取会让持有 `SEMANTIC_VIEW` 的人
      直接读到密封题面，使 95% 门禁失效。
- [x] **SEM-READ-003（新）— 已裁定：不实现样本级读取接口，改为分片曝光声明。**
      「读取即退役该样本」的接口不应存在：任何能显示密封题面的通道，都让持有
      `SEMANTIC_VIEW` 的人可以一条一条把密封集读空——退役副作用只让它**可审计**，
      并不让它**安全**，而 95% 门禁恰恰依赖这些题面从未被看过。
      且该粒度与既有设计相悖：`evaluation_shard_states` 的退役单位一直是**分片**，
      `evaluation.RecordSealedRead` 也按分片退役。
      **真实缺口是曝光无法被记录**：`askdata.expose_evaluation_shard`（SQL，含权限校验且幂等）
      与 `ShardRetiredExposed` 常量都已存在，却没有任何调用方，
      运维发现泄露时无处登记，`retire_reason='EXPOSED'` 不可达。
      现已补齐 `GetEvaluationShardHealth` / `ExposeEvaluationShard`、
      `GET|POST /evaluation-sets/{id}/shards[/expose]` 与运营工作台面板：
      只展示计数与轮换状态、剩余可用分片和能否签发 95% 结论，
      曝光声明为两步不可撤销操作，**任何界面仍不显示密封题面**。
- [x] **WEB-007** — 语义管理与发布页面：已交付模型、指标、维度、知识、导入导出、认证、Release Readiness/投影/评测/双人审批/激活，以及反馈工单和主动学习运营视图；密封题面继续遵守不暴露原则。
- [x] **SEM-DICT-WIRE-001（新）** — 认证业务词典接入查询期：`dictionarypostgres.Loader` +
      `understanding.DictionaryMatcher` + `dictionarysearch.ExactHits` 此前是死代码，
      词条可以认证发布但从不参与回答。现已在 `cmd/worker` 构造并注入
      `tools.Services.Dictionary`，命中以确定性精确命中进入检索
      （`RetrievalRequest.DeterministicExact`），压过词法相似度。
      词典缺失或报错只降级检索精度，不让问题失败。
- [x] **SEM-QUALITY-001（新）— 已裁定：`rule_ast` 是「绑定」，不是新的表达式语言。**
      裁定依据：平台**已经有**一套真正在跑的数据质量执行链——物化 Worker 产出
      `platform.data_quality_results`，并以质量失败阻止切换快照。
      再造一套表达式语言等于再造第二个求值器，且会让 Owner 创作出无人执行的规则，
      `get_data_quality_status` 会从 UNKNOWN 变成「规则存在但未通过」，
      把「从未检查」误报成「检查不合格」。
      四个必答问题的裁定：**能表达什么** = 执行目录中的一项检查 + 作用域 + 目标；
      **谁执行** = 物化 Worker，语义层只读结果，绝不自行求值；
      **何时执行** = 产出物化的数据集构建运行期间；
      **针对哪个快照** = 语义模型经 `materialization_id` 固定的 ACTIVE 物化，
      与查询实际读取的快照完全一致。
      落地：`materialization.ExecutingQualityRules` 为唯一执行目录（Worker 已改用同一常量）；
      `DATASET_QUALITY_BINDING` 解码器在**写入、认证、Release 门禁**三处拒绝
      绑定到不被执行的检查（新增 `RELEASE_QUALITY_RULE_NOT_EXECUTABLE`）；
      新增 `quality-rules` 完整 CRUD 创作路径；认证规则随 Release 清单一同冻结；
      `DataQuality` 按固定物化解析出真实 `PASSED/FAILED/SKIPPED/UNMEASURED`
      并支持 `maxAgeHours` 时效约束；迁移 `000334` 提供库级底线。质量运营页现已提供
      新建、编辑、删除草稿、保存并认证以及执行/失败语义说明；真实浏览器已完成
      “创建阻断级规则 → 认证 → 组成 rc2 → 4/4 投影 READY”。认证候选解析、锁定、
      更新状态、依赖排序和草稿删除也已覆盖 QUALITY_RULE，避免出现能创建却不能认证的半链路。
      **顺带修正一处潜伏合同缺陷**：`qualityStatusFor` 原先返回 `PASSED/PARTIAL`，
      而它们**不在**冻结的 `get_data_quality_status` 枚举（`PASS/WARNING/FAIL/UNKNOWN`）内——
      只因此前无法创作规则而从未触发，一旦有规则即被 `ValidateResult` 拒绝。
      现按 `FAIL > WARNING > UNKNOWN > PASS` 定序，未测量者绝不读作通过。验收中还发现并
      修复了受治理 Measure 统一投影为检索 `METRIC` 后数据库主体校验不一致的生产断点
      （迁移 `000335`），失败投影可从诊断面板原位重试并恢复 READY。
- [ ] **EVAL-008** — 时间类专项黄金用例集。
- [ ] **EVAL-009** — 可加性专项黄金用例集。
- [ ] **EVAL-010** — 叙述人工评审集。

> 关于 `internal/askdata/evaluation/suites`：该包不被任何 `cmd` 链接，
> 但这**不是接线缺陷**。它是纯评测库（可加性、时间、叙述、Cohen's Kappa 一致性），
> 入参是外部提供的黄金用例集，本身有 9 项通过的单元测试。
> 缺的是用例，而用例属于业务输入（HUMAN-001～004 阻塞 EVAL-*）。
> 在业务方提供黄金集之前搭一个无料可跑的评测入口没有意义；
> 按 §9 的纪律，此时只能建设合同与校验器，不得编造业务答案。

## 4. M4 报告主链（P11）

> 一期 MVP 目标：报告主链在**未配置模型提供方**时端到端可用。
> 组件使用 `DATASET_FIELD` 绑定时只依赖已发布数据集版本，不需要 Release ACTIVE。

- [x] **RPT-BLANK-001** — 空白报告新建：`POST /api/v1/reports/blank` + `GET /api/v1/report-data-contexts`，
      `/reports/new` 提供「创建空白报告」与「让 AI 生成」两条并列入口。
- [x] **RPT-PUB-FALLBACK-001** — 发布评审在缺少模型提供方或模型评审失败时回退为
      `DeterministicPublishReview`，裁决与门禁一致，来源记入审计与响应 `review.source`。
- [x] **WEB-RPT-003** — 属性与数据绑定面板：按组件模板合同（`GET /api/v1/report-component-manifests`）
      限制角色与维度/度量数量，字段来自服务端裁剪结果；`SEMANTIC_IR` 绑定只读。
- [x] **WEB-RPT-002** — 报告设计器画布与大纲（结构树与分区排序仍待补）。
- [x] **WEB-RPT-004** — AI Dock 与差异预览（差异详情只展示真实 Operation 内容）。
- [x] **WEB-RPT-006** — 发布评审、版本与回滚界面。
- [x] **WEB-RPT-007** — 报告资产中心。
- [x] **RPT-006 / RPT-008 / RPT-011 / WEB-RPT-008** — 运行时结果绑定、筛选交互、导出与分享前端接线。
- [x] **WEB-RPT-001** — 模板中心与报告创建向导：内置真实模板目录、受治理数据上下文选择和模板实例化接口。
- [x] **WEB-RPT-005** — 移动端预览与降级：发布评审提供移动端预览，发布前固定移动适配 Hash 并执行响应式门禁。
- [x] **WEB-RPT-009** — 设计器内的组件增删与布局编辑：支持组件库添加、删除、拖拽、缩放、章节排序与冲突推移保存。

## 5. M5 报告资产进入对话（P12 + P09/P10）

- [x] **FUSE-006** — 问数 LLM 发现、运行并引用已有报告资源。
  - 2026-08-12：Tool Host 新增 `REPORT_ASSET` 闭集类型，认知检索默认并行发现当前 Release 的认证报告组件；每个候选在暴露前再次通过 `report_v2_can_access(VIEW)`、当前发布版本、认证/投影状态和 Release/Hash 一致性校验。报告仅作为展示先验与来源身份，主链仍重新绑定当前语义合同、编译、校验并执行查询，不复用历史 SQL 或自由文本。答案最多保留 3 项达到相关性门槛的不可变来源身份。
- [x] **WEB-017** — 问数中的报告来源卡、引用状态与受权跳转。
  - 2026-08-12：结果合同新增隐私安全的认证报告来源数组；证据栏展示报告/组件/版本、已引用状态和“基于当前 Release 重新执行”的信任说明，并生成受控原报告版本跳转。未命中或无权访问时不显示卡片，不泄露报告标题或存在性。
- [x] **WEB-014** — 真实会话历史、搜索、置顶、重命名、归档与断线恢复。
- [x] **WEB-015** — 问数结果真实资产动作栏：导出、分享、反馈、加入报告和形成决策均已接入真实接口。
- [x] **WEB-016** — 业务分析首页与个人分析资产入口。
- [x] **WEB-008** — 时间区间与 asOf 统一展示，前后端共享时间口径夹具。
- [x] **WEB-009** — 合计行、堆叠限制与不可加提示。
- [x] **WEB-010** — 保存问题、收藏与团队共享：已支持从已验证运行由服务端提取受控 Semantic IR，原子保存并按团队授权共享；首页与问数会话栏均可查看、重跑和归档收藏，重跑沿用原发布版本与口径并完成真实异步运行回归。
- [x] **WEB-012** — KPI Bundle 结果视图。
  - 2026-08-12：单行多指标结果自动投影为 `KPI_BUNDLE`，公共合同与浏览器双重约束 2–8 个数值指标；工作台使用同一口径指标卡网格展示并保留明细追溯，已完成浏览器快照与 1920 基线响应式走查。
- [x] **WEB-013** — 降级、配额与不可用状态：终态原因、关系图降级、Release 漂移、超额和恢复动作均显式呈现。

## 6. 平台与运营（M4～M6 并行）

- [x] **PLAT-UX-001** — 面向业务人员的平台定位、导航与权限分流。
- [x] **PLAT-LIFE-001** — 用户停用、Owner 转交与孤儿资产防护：已接入 `deactivation-preview`、原子停用批次与失败重试接口；页面展示转交、自动关闭、只读保留与阻断事项，并为每类资产绑定领域内有效接收人。成功后即时撤销会话并递增令牌版本，后端集成测试覆盖成功、失败回滚、最后管理员保护和认证内容不可变。
- [x] **WEB-OPS-001** — 反馈、主动学习与语义质量运营工作台：负反馈自动建单、候选聚类、修复审批、Release 关联和回归用例门禁已接通。
- [x] **WEB-OBS-001** — 运营、可观测、配额与成本治理页。
  - 2026-08-12：新增平台管理员专属运行观测接口与原生 CSS 控制面，真实聚合 AI 日/月配额、Token/成本水位、成功率与 P95 延迟、问数回答/阻断/运行状态、四类异步队列积压/失败/最长等待、用途分布和稳定失败码；接口不绑定业务领域且仅输出隐私安全的计数与代码。完成 1h/6h/24h/7d 切换、健康分级、无横向溢出回归与生产构建验证。
- [x] **OPS-008** — 运行配置版本与受控变更中心（页面）：定义、草稿、审批、灰度、回滚和部署参数均已接入。
- [x] **PERF-001** — 前端路由级分块与性能预算：页面已按路由懒加载，主入口约 260 KB / gzip 83 KB；ECharts 渲染器保持独立按需分块。
- [x] **UI-MIGRATE-001** — element-plus / Ant Design → 原生 React + CSS 组件收口：依赖、锁文件与源码中
      不再有 element-plus、Vue 或 antd 引用；统一按钮入口为 `web/src/components/AppButton.tsx`
      （底层原生 `button`，以自有 CSS 保留 `variant/plain/text/link/round/circle` 业务语义）；
      `main.tsx` 不再注入第三方组件主题，所有已重构页面选择器统一使用 `.app-button`。
      登录、首页、领域访问、用户权限、报告中心与数据源页已在 1920×1080 回归；
      `npm run lint`、`npm run test`、`npm run build` 全部通过。

### 6.1 运行期发现（实际启动 Worker 后观测到）

- [x] **OPS-OUTBOX-001** — 报告资产抽取 outbox 存在**孤儿行**：本地库 20 行
      `askdata.report_asset_extraction_outbox` 指向的 `report_version_id` 在
      `platform.report_versions` 中已不存在（多半是历史清理或重置留下的）。
      `Extract` 对这些行执行 `QueryRow(...).Scan(...)` 得到 `pgx.ErrNoRows`，
      按普通错误上抛，Worker 每个轮询周期重试并打一条 ERROR。
      实测空载 35 秒产生 100 条 `project report semantic asset` 错误日志。
      虽然 `attempt<10` 使其自限，但（1）噪声会淹没真实错误——包括新接入的
      问数运行 Worker 的失败；（2）「no rows in result set」对运维没有任何可操作信息。
      **已修复**：新增 `ErrAssetSourceGone`，`Extract` 在目标版本缺失时返回该永久性错误；
      `FinishExtraction` 将其 `attempt` 直接置为 10（现有 `attempt<10` 领取谓词即终结条件，
      无需新增状态），并记 `error_code='REPORT_ASSET_SOURCE_GONE'` 保留审计原因；
      `ProcessNext` 对这一已处置情形不再上报为 Worker 错误。瞬时故障照常上报，不被吞掉。
      已对运行中的系统验证：单个孤儿行由 attempt 6 一次跳至 10 并记为 SOURCE_GONE，
      同期抽取错误日志由 100 条/35 秒降为 0。
- [x] **OPS-PERM-001** — 幂等记录清理每轮报 `permission denied (SQLSTATE 42501)`，
      清理从未真正执行过，过期记录会无限累积。
      **原描述不准确**：`report_worker` 对 `askdata.idempotency_records` 是有
      `SELECT,DELETE` 的，缺的是 `UPDATE`——而清理用的是 `SELECT ... FOR UPDATE SKIP LOCKED`，
      行锁需要 `UPDATE` 权限。已实测复现：不带 `FOR UPDATE` 可查，带上即 42501。
      **修复取最小权限方向**：去掉 `FOR UPDATE SKIP LOCKED`，而不是给 Worker 授予 `UPDATE`。
      授予 `UPDATE` 会让清理进程有能力改写幂等记录，这是它绝不应该具备的能力；
      而行锁对正确性并非必需——后续 DELETE 以 `id + expires_at` 为条件，
      两个 Worker 抢同一行时一个删除成功、另一个影响 0 行。
      已对运行中的 Worker 验证：空载 25 秒 ERROR 日志由 127 条降为 0 条。

## 7. M6 门禁与生产化

- [x] **E2E-P1-001** — 一期「报告先设计、对话后引用」端到端门禁：真实问数结果已完成加入报告、执行、发布、分享、导出和后续决策闭环。
- [ ] **OBS-003** — 容量基线与压测（`scopeProfile=P1_REPORT_ASKDATA`）。
- [ ] **OPS-003** — 数据库和图备份/重建演练。
- [ ] **OPS-004** — 生产容量与故障 POC。
- [ ] **OPS-P1-001** — 一期报告/问数混合容量与恢复签署。
- [ ] **OPS-007** — 平台级容量、灾备与业务连续性扩展。
- [ ] **PILOT-001** — Shadow。
- [ ] **PILOT-002** — 5%/20%/50% Canary。
- [ ] **PILOT-003** — 95% 正式验收。

## 8. 后期规划（一期之后，不阻塞签署）

- [x] **WEB-DEC-001** — 决策中心、审批、行动看板与复盘页（页面已交付并进入导航；业务验收仍属后期）。
- [x] **TASK-UI-001** — 统一任务中心页面（页面已交付并进入导航；业务验收仍属后期）。
- [x] **E2E-BIZ-001** — 业务人员「分析 → 固定证据 → 决策 → 审批 → 行动 → 复盘 → 关闭」端到端旅程门禁。
- [x] **FUSE-WRITE-UI** — 问数写回报告前端：选择报告/章节、布局预览、确认写回和新建报告分支已完成。
- [x] **SUB-UI-001** — 报表订阅与定时分发页面：计划创建、自订阅、暂停/恢复、补发、送达记录与安全打开已完成。
- [ ] **PILOT-004** — 第二业务域复制模板。

## 8.1 2026-08-12 产品全链稳定性收口

- [x] 数据资产真实链路回归：已发布 Excel/CSV 数据源可进入资产目录与单表完善页；元数据 AI 建议接口完成生产注册，AI 辅助失败不再阻断基础元数据；脱敏样本真实返回并准确显示 5 行。
- [x] 数据集真实链路回归：目录可加载真实 ODS/DWD/DWS 资产；DATE_TRUNC 分组输出保留 `TIME` 角色，消除 `analysisContract.timeField` 伪失败；真实 DWS 草稿完成“修改 DAG → 校验 → 语义校正 → 保存”并由 V2 升为 V3。
- [x] 报告真实链路回归：模板创建、字段绑定、编辑、发布评审、发布、运行、订阅、补发与报告中心读取已贯通；旧版未显式声明 `zone.order` 的不可变报告继续按原稳定顺序读取，不改写历史制品 Hash。
- [x] 异步任务恢复：迁移 `000337` 将仍有 READY 来源的历史报告资产抽取失败重新入队，将无源记录归档为 `REPORT_ASSET_SOURCE_GONE`；运行 Worker 已验证有效版本一次重试转为 `DONE`，不再产生持续假告警。
- [x] 发布门禁：`go test ./...`、前端 91 项测试、lint、生产构建、`make ci-check`、数据库权限/迁移验证、独立数仓验证和 `git diff --check` 全部通过；登录后 17 个核心操作路由批量浏览器回归无崩溃页、无告警页。

> 上述收口表示当前仓库内可实现、可自动验证的产品链路已完成。`E2E-DATA-001` 的外部 MySQL/Oracle 环境签署、正式黄金集、目标生产容量/灾备和真实业务 Canary 仍保留为环境/业务验收项；不得用本地回归数据伪造这些生产结论，也不阻塞本地完整产品运行。

## 9. 人工输入门禁（业务方提供，阻塞对应任务）

| 门禁 | 内容 | 阻塞 |
|---|---|---|
| HUMAN-001～004 | 首个业务域、核心指标口径、维度与成员策略、黄金问题集 | M2、EVAL-*、PILOT-003 |
| HUMAN-005 | 生产 Nebula 容量/TLS/备份/副本 | OPS-004/007 |
| HUMAN-006 | 语义发布双人审批人 | Release 激活 BIZ |
| HUMAN-007 | 业务日历与时间策略 | TIME 链 BIZ |
| HUMAN-008 | 指标可加性确认 | ADD-002 |
| HUMAN-009 | 报告模板与叙述规范 | M4 |
| HUMAN-010 | 报表资产认证责任人 | M5 |
| HUMAN-011 | 明细取数审批链 | DR 链 BIZ |
| HUMAN-012 | 配额与成本策略 | M6 |
| HUMAN-013 | 目标环境规模、SLO 与水位 | OBS-003、OPS-004/007 |
| HUMAN-014 | 决策治理规则（后期） | WEB-DEC-001 BIZ |
| HUMAN-015（新） | 业务补充信息**内容**（机制均已就绪）：指标口径说明、维度值定义、数据过滤规则，以及行级安全的「谁对应哪个数据范围」授权清单 | SEM-CTX-001 BIZ |

未提供人工输入前，可以建设合同、校验器、导入工具和测试夹具，**但不得编造业务答案**。
