# 智能化报告生成系统 TODO 开发计划

> 执行对象：Codex  
> 需求基线：`docs/intelligent-report-system-design-v1.0.md`  
> 核心业务后端：Go（Golang）；数据库连接层：独立 Python Connector Service  
> 当前状态：工程基座、租户权限、数据源与资产中心主体已落地；数据集 DSL V1、安全查询编译器、带 Join 风险提示的跨源实时预览，以及报告 JSON、统一渲染器、组件库、筛选联动纯解析和服务端报告草稿持久化已落地，缓存/物化、指标、不可变发布版本与在线运行尚未完成
> 最近审计：2026-07-16，已按代码、迁移、自动化测试和本地 Docker 实际运行结果复核
> 计划原则：先打通可运行的纵向闭环，再扩展智能生成、跨源查询、调度和公开能力

---

## 1. Codex 执行规则

### 1.1 每次领取任务前

- 阅读本文件、需求文档及仓库中的 `AGENTS.md`（若后续新增）。
- 一次只领取一个可独立验收的任务；不得把多个阶段一次性混在同一变更中。
- 检查当前工作区状态，保留用户已有修改，不覆盖无关文件。
- 若任务依赖尚未完成，先完成依赖任务或明确报告阻塞，不用临时假实现掩盖依赖。
- 涉及数据库、对象存储、消息队列、联邦查询引擎或云模型时，先提供本地可替代实现和清晰接口边界。

### 1.2 每个任务必须包含

- 实现代码；
- 单元测试或集成测试；
- 必要的数据库迁移；
- API/Schema/配置文档更新；
- 本地验证命令；
- 对安全、租户隔离和版本兼容性的检查。

### 1.3 完成定义（Definition of Done）

任务只有同时满足以下条件才能标记为 `[x]`：

1. 需求和验收条件全部实现；
2. 格式化、静态检查、单元测试和相关集成测试通过；
3. 不包含明文凭证、临时调试代码或无法解释的跳过测试；
4. 新增配置均提供默认值和示例；
5. 新增 API 有请求、响应及错误码说明；
6. 新增持久化结构有向前迁移方案；
7. 多租户数据访问包含 `tenant_id` 校验；
8. Codex 在任务交付说明中列出修改文件、验证结果、遗留风险和下一建议任务。

### 1.4 建议统一验证命令

工程初始化后应提供统一入口，目标命令如下：

```bash
make fmt
make lint
make test
make test-integration
make build
```

前端初始化后增加：

```bash
make frontend-lint
make frontend-test
make frontend-build
```

---

## 2. 目标工程结构

以下结构由初始化任务创建，可在技术验证后微调：

```text
.
├── cmd/
│   ├── api/
│   ├── worker/
│   └── migrate/
├── internal/
│   ├── auth/
│   ├── tenant/
│   ├── datasource/
│   ├── metadata/
│   ├── dataset/
│   ├── metric/
│   ├── report/
│   ├── renderer/
│   ├── ai/
│   ├── scheduler/
│   ├── storage/
│   └── audit/
├── migrations/
├── api/
├── web/
├── configs/
├── deployments/
├── test/
├── docs/
├── Makefile
└── go.mod
```

---

## 3. 里程碑总览

| 里程碑 | 目标 | 退出条件 |
|---|---|---|
| M0 工程基座 | 建立可构建、可测试、可迁移的 Go/前端工程 | API、前端、PostgreSQL 本地启动且 CI 通过 |
| M1 租户与数据源 | 完成租户隔离以及 MySQL、Oracle、Excel 接入 | 三类数据源可创建、测试/解析和同步资产 |
| M2 数据集与指标 | 完成单源、跨源数据集和指标闭环 | 数据集可查询，指标可试算和版本化 |
| M3 报告设计器 MVP | 完成长画板、分块和组件编辑 | 配置可保存为 JSON 并由渲染器还原 |
| M4 AI 报告能力 | 完成整份报告生成和分块 AI 修改 | AI 变更可预览、校验、确认、撤销和审计 |
| M5 发布与运行 | 完成版本、权限、公开访问、缓存和容错 | 已发布报告可安全在线访问 |
| M6 调度与归档 | 完成定时运行、邮件、PDF 和历史归档 | 定时任务可生成并发送可追溯 PDF |
| M7 生产就绪 | 完成性能、安全、监控和灾备 | 核心验收项和上线检查全部通过 |

### 3.1 当前完成度审计（2026-07-15）

| 范围 | 审计结论 | 说明 |
|---|---|---|
| M0 工程基座 | 已完成 | Go、前端、CI、迁移和基础容器均可运行；Connector 本地测试依赖说明仍需回补 |
| M1 租户与数据源 | 主体完成，未达到完整退出条件 | 租户、认证、RBAC、MySQL、Oracle、Excel/CSV、资产中心和元数据 AI 手动补全链路已实现；公开策略、查询阶段行列权限、真实元数据同步/模型端到端验收及 AI 异步批量触发仍缺失 |
| M2 数据集与指标 | 已启动 | T0301-T0303 已完成；T0304 已支持三类跨源组合的受限实时预览、设计器建模和 Join 风险提示，缓存/物化与指标尚未完成 |
| M3 报告设计器 MVP | 核心完成 | T0401-T0409 已完成正式报告 JSON 合同、统一渲染器、长画板、动态网格、组件与交互、浏览态冻结，以及租户隔离的服务端草稿、Patch 审计、乐观锁和冲突恢复；不可变发布与精确版本数据查询归入 M5 |
| M4 AI 报告能力 | 未开始 | 已提供环境变量示例，但尚无模型 Provider、AI 服务或分块 AI 浮窗 |
| M5 发布与运行 | 未开始 | 未实现报告版本、发布、公开访问、缓存和在线运行服务 |
| M6 调度与归档 | 未开始 | Worker 仅有工程入口，尚无任务、调度、邮件或 PDF 业务实现 |
| M7 生产就绪 | 未开始 | 已有基础日志和部分安全测试，但未形成监控、压测、灾备和完整安全矩阵 |

按 T0001-T0806 共 53 个核心任务统计：可认定核心完成 20 项（T0001-T0004、T0101-T0103、T0201、T0301-T0303、T0401-T0409）；部分完成或实现完成但验收待补 8 项（T0005、T0104、T0202-T0206、T0304）；其余 25 项尚未开始。T0409 新发现的逆向补偿证明、新建入口旧草稿迁移边界和容量策略已分别留在本节、T0802/T0803，不据此误报发布链路完成。另有 T0901-T0910 共 10 项审计回补任务，其中 T0909 已完成，T0908/T0910 部分完成，其余 7 项未完成。

---

## 4. P0：工程基座与架构验证

### T0001 初始化 Go 工程（已完成）

- [x] 创建 Go Module、`cmd/api`、`cmd/worker` 和内部模块目录。
- [x] 实现配置加载、结构化日志、错误码、请求 ID、优雅退出和健康检查。
- [x] 提供 `/health/live`、`/health/ready`。
- [x] 创建 `Makefile` 的 `fmt/lint/test/build` 命令。
- [x] 增加基础单元测试。

验收：空环境可构建 API 和 Worker；健康检查返回明确状态。

### T0002 初始化前端工程（已完成）

- [x] 创建 `web` 工程并确定 TypeScript、路由和测试框架；状态管理保持 React 局部状态，待跨页面业务状态出现后再引入专用库。
- [x] 建立登录页、管理后台壳、设计器壳和在线查看器壳。
- [x] 建立统一请求客户端、错误提示和主题变量。
- [x] 提供 lint、单元测试和生产构建命令。

验收：四类页面可路由访问，前端测试和构建通过。

### T0003 建立本地开发环境（已完成）

- [x] 提供 PostgreSQL、缓存、对象存储的本地容器编排。
- [x] 提供 `.env.example`，不得提交真实密钥。
- [x] 实现数据库迁移命令；测试数据初始化将在 T0101 基础租户模型落地后补充。
- [x] 编写本地启动、停止、重置和排障说明。

运行验收状态（2026-07-15）：PostgreSQL、Redis、MinIO 全部健康，三个 MinIO 存储桶创建成功；迁移与数据库验证通过。

验收：新开发者按照文档可在一个工作日内启动完整基础环境。

### T0004 建立 CI 基线（已完成）

- [x] 配置 Go 格式化、静态检查、测试和构建。
- [x] 配置前端 lint、测试和构建。
- [x] 配置迁移文件一致性检查、依赖漏洞和密钥扫描。
- [x] 禁止测试失败时继续发布制品。

验收：故意引入格式、测试或密钥问题时 CI 能正确阻止合并。

### T0005 技术验证：Oracle、Excel、联邦查询与页面渲染（部分完成）

- [x] 验证 Oracle 连接、超时、取消和许可证条件（数据库连接层已切换为 Python `oracledb` Thin 模式，不依赖 Oracle Client/OCI；Oracle Free 23.26.2 真实查询通过）。
- [ ] 验证 `.xlsx`、`.xls` 的工作表读取、公式值、日期和大文件内存占用（`.xlsx` 自动化完成，`.xls` 适配器完成但复杂样本待补）。
- [ ] 对比独立联邦引擎与 Go 查询网关方案，记录 MySQL/Oracle/Excel Join 的执行结果（Go Hash Join 基准完成，独立引擎和真实源待对照）。
- [ ] 验证统一报告 JSON 在设计器、在线查看和 PDF 渲染中的一致性（合同一致性完成，真实 PDF 视觉验证待导出 Worker）。
- [x] 输出 ADR，明确首期采用方案和不采用方案。

验收：形成可复现原型、压测数据、风险清单和架构决策记录。

---

## 5. P1：多租户、身份与权限

### T0101 建立控制库基础模型（已完成）

- [x] 创建租户、用户、角色、用户角色、权限、审计日志表。
- [x] 所有租户业务表统一包含 `tenant_id`，核心实体包含创建/更新时间和版本字段。
- [x] 为租户字段、状态字段和常用查询建立索引。
- [x] 建立只向前迁移和前向修复策略。

运行验收状态（2026-07-15）：迁移 SQL 已在 PostgreSQL 17 成功执行；`report_admin`/`report_app` 账号分离、双租户 RLS、跨租户关联阻断和审计不可变全部通过数据库验证。

验收：两个租户可创建同名资源且互不可见。

### T0102 实现身份认证（已完成）

- [x] 实现登录、退出、访问令牌、轮换刷新令牌和 bcrypt 密码安全策略。
- [x] 从服务端验证通过的身份上下文解析 `tenant_id`，禁止普通请求覆盖。
- [x] 实现禁用用户、Token Version、会话撤销和登录/退出审计。

验收：伪造租户 ID、过期令牌和禁用用户均不能访问业务 API。

### T0103 实现 RBAC 与对象权限（已完成）

- [x] 实现功能权限、对象权限和操作权限核心模型。
- [x] 提供平台管理员、租户管理员、数据管理员、分析师、报告设计师和查看者基础角色种子。
- [x] 将权限中间件接入权限管理 API，并补齐角色、授权、撤销管理 API 与数据库集成测试；后续业务模块必须复用同一中间件。

验收：角色矩阵自动化测试覆盖允许和拒绝路径。

### T0104 实现行级与列级权限框架（核心框架与查询接入完成，端到端验收待补）

- [x] 定义行级策略 DSL 和列级隐藏、掩码、仅聚合策略。
- [x] 从数据库加载适用策略并在真实查询编译阶段注入，不依赖前端过滤。
- [x] 缓存键包含租户、用户或权限版本。

验收：同一数据集对不同用户返回不同授权结果，缓存不串租户。

### 阶段回补清单

- [x] 建立真实 PostgreSQL RBAC、对象权限和策略集成测试。
- [x] 完成角色、角色权限、用户角色、对象授权与撤销 API。
- [x] 强制校验对象权限主体属于当前租户。
- [x] 实现策略仓库、用户/角色适用范围匹配和查询执行接入。
- [ ] 补充同一数据集由两个真实用户执行并返回不同授权结果的 HTTP/Connector 端到端验收；当前策略仓库、编译器和运行时分别有自动化测试，但尚未覆盖完整网络链路。
- [x] 执行 `AGGREGATE_ONLY` 最小分组规模约束。
- [x] 完成前端路由守卫、Bearer Token、自动刷新和服务端注销。
- [x] 建立 CI、迁移检查、依赖漏洞与密钥扫描。

### T0105 实现对象可见性与公开策略模型

- [ ] 支持 `PRIVATE`、`TENANT`、`PUBLIC`。
- [ ] 支持公开地址、访问码、有效期、禁止下载和撤销公开。
- [ ] 建立公开发布安全校验和访问审计。

验收：公开访问只能读取已发布版本，不能访问后台或敏感依赖。

---

## 6. P1：数据源与数据资产

### T0201 实现统一数据源接口

- [x] 定义 Go 领域层与 Python 数据连接服务之间的统一接口。
- [x] 实现创建、更新、启停、连接测试、同步和删除状态机（Go 领域层、PostgreSQL/RLS 持久化及 HTTP API 已接入）。
- [x] 敏感凭证只保存 `secret_ref` 或加密值。
- [x] 为每个租户定义连接池、并发和文件大小配额（数据库连接池和租户查询并发已强制执行；Excel 文件大小在 T0204 收口）。
- [x] 收口状态机和连接生命周期：草稿/错误状态不能绕过测试，删除时关闭 Python Pool，空闲 Pool 自动淘汰。

验收：新增数据源类型不需要修改上层数据集核心接口。

### T0202 实现 MySQL 数据源（功能完成，端到端验收待补）

- [x] 支持连接配置、连接测试和双层只读校验（失败关闭词法防护 + 源账号只读授权；完整 AST 重写归入 T0303/T0305）。
- [x] 同步 Catalog、表、视图、字段、注释、主外键和索引摘要。
- [x] 支持基于结构哈希的差异同步和失效标记。
- [ ] 使用真实 MySQL 完成“Go 数据源服务 → Python Connector 元数据同步 → PostgreSQL 快照/资产/差异”的端到端测试，并纳入 CI。

验收：满足需求文档 `DS-01`、`MD-01`、`MD-02`。

### T0203 实现 Oracle 数据源（功能完成，端到端验收待补）

- [x] 支持 Service Name/SID、Schema 范围和连接测试。
- [x] 同步表、视图、字段、注释、约束和索引摘要。
- [x] 处理 Oracle 空字符串、NUMBER、DATE 和大小写差异，并提供显式查询取消。
- [ ] 使用真实 Oracle 分别覆盖 Service Name、SID、Schema 范围、元数据持久化、超时和取消的端到端测试，并纳入 CI 或独立夜间流水线。

验收：满足需求文档 `DS-02`，连接取消和超时可控。

### T0204 实现 Excel 数据源（主体完成，样本与 HTTP 验收待补）

- [x] 支持上传 `.xlsx`、`.xls`、`.csv`，CSV 支持 UTF-8/GBK/GB18030、逗号/分号/Tab/单字符分隔符及自定义引号方言；在 MinIO 保存原始文件并记录 SHA-256。
- [x] 支持选择工作表、表头行、空行策略和列名去重规则。
- [x] 推断文本、整数、小数、布尔、日期和时间类型，支持人工覆盖。
- [x] 将工作表映射为表资产、列映射为字段资产。
- [x] 重新上传生成不可变文件版本并输出工作表/字段差异。
- [x] 为已发布数据集提供不可变文件版本 ID；数据集外键在 T0301 数据集模型中接入。
- [ ] 补充复杂 `.xls` 样本、Multipart HTTP 上传/重传、损坏 GBK/GB18030、超大 CSV 和公式注入安全测试。

验收：满足需求文档 `DS-03`；异常文件、超限文件和公式错误有明确提示。

### T0205 实现数据资产中心（功能完成，集成验收不足）

- [x] 实现表资产、字段资产、业务描述、标签、敏感级别和人工锁定。
- [x] 实现搜索、筛选、详情、版本差异和下游影响入口。
- [x] 实现租户内目录公开与只读复用规则。
- [ ] 补充资产业务元数据更新、业务版本冲突、人工锁定字段同步保护、目录公开、差异查询和影响分析的 PostgreSQL/API 集成测试；当前仅有跨租户读取隔离测试和模型校验测试。

验收：数据管理员可完成资产查找、补全、锁定和变更追踪。

### T0206 实现元数据 AI 补全（主体完成，真实模型与异步批量验收待补）

- [x] 定义 OpenAI-Compatible 模型供应商抽象层和严格结构化输出 Schema；本地校验为最终安全边界。
- [x] 对表字段生成业务名称、业务描述、标签、语义类型、敏感级别建议和置信度。
- [x] 低置信度、人工锁定和生成期间版本变化结果进入待确认；支持显式接受/拒绝且再次执行锁与版本检查。
- [x] 记录 Provider、模型、提示词版本、Token、输入哈希、延迟、结构化结果、状态和审计信息，不落 API Key、原始提示词或失败响应正文。
- [x] 增加非法/缺失/虚构资产 ID、非法标签与语义类型、超时、人工锁定、低置信度、跨租户隔离和建议决策测试；PostgreSQL 迁移与集成测试已通过。
- [ ] 使用真实受控模型完成连通性、Schema 兼容性、超时和提示词质量验收，并建立 Token/费用预算、租户限额与限流策略。
- [ ] 将当前“按表手动同步调用”扩展为元数据同步后的异步批量任务，补齐幂等、重试、取消和失败恢复，避免同步接口被模型延迟阻塞。
- [ ] 将内置基础标签枚举扩展为租户可配置标签字典，同时保持输出 Schema 白名单约束和历史结果兼容。

验收：模型输出非法、超时或虚构内容时不会污染正式资产。

验收证据：`internal/metadataai` 单元测试覆盖严格结构化输出、未知/缺失字段拒绝、超时失败和锁/版本/置信度分流；`test/integration/metadata_ai_test.go` 覆盖正式资产应用、待确认、人工决策、Token/审计记录与 RLS 隔离。当前未配置真实模型密钥，真实 Provider 质量和运营限额仍待验收。

---

## 7. P2：数据集、查询与指标

### T0301 定义数据集 DSL V1

- [x] 定义节点、Join、字段、过滤、聚合、排序、参数和输出粒度。
- [x] 提供 JSON Schema、版本迁移器和示例。
- [x] 实现 DSL 保存、加载和确定性规范化。

验收：同一 DSL 可稳定生成相同规范结构和查询计划。

验收证据：`internal/dataset` 覆盖严格解析、旧粒度字段迁移、引用/Join/粒度校验、确定性 DSL/计划哈希和乐观锁服务测试；`test/integration/dataset_test.go` 覆盖 PostgreSQL 草稿保存、派生字段/参数/血缘索引、并发冲突和跨租户 RLS 隔离。正式 Schema 与样例位于 `api/schemas`、`api/examples`。

### T0302 实现单源数据集设计器（已完成）

- [x] 支持选择同一数据源内的表、字段、Join、过滤、聚合、计算字段、排序和参数。
- [x] 支持小样本预览和错误定位；MySQL/Oracle 使用安全参数化查询，Excel/CSV 使用固定文件版本受控执行器，设计器不直连 Connector 或对象存储。
- [x] 保存 DSL，不把生成 SQL 作为唯一事实来源，并支持按数据集 ID 重新加载编辑和乐观锁更新。
- [x] 增加租户隔离的数据集摘要列表 API，供设计器入口和后续目录页使用。
- [x] Excel/CSV 资产合同返回当前不可变 `fileVersionId`，设计器将其固化到 DSL TABLE 节点。

验收：MySQL、Oracle、Excel 均可创建单源数据集并重新加载编辑。

遗漏依赖审计：原计划把“小样本预览”放在 T0302、把安全查询编译器放在 T0303，但前者必须依赖后者。现已完成依赖回补：数据库预览走参数化 SQL，Excel/CSV 走独立文件 DSL 求值器，并在同一运行时统一权限、超时、取消、行数限制和审计。

语义遗漏修复：`SINGLE_SOURCE` 的含义是全部物理表来自同一数据源，并非只能包含一个表；已修正 DSL 校验和设计器类型推导，增加同源多表 Join 回归测试。

当前证据：`web/src/pages/DatasetDesignerPage.tsx` 已接入资产目录、字段配置、Join、过滤参数、计算、排序、粒度、校验、创建、重载、预览参数、结果表格和取消；`web/src/lib/datasets.test.ts` 覆盖 DSL 与预览参数构建。`internal/filequery` 覆盖固定版本字段校验、过滤、聚合、排序、行列策略、缺省可选参数、结果上限和取消；Excel/CSV 读取会复核对象大小与 SHA-256，集成测试证明当前版本切换后仍按 v1/v2 标识读取各自表头。真实 MySQL/Oracle Connector 已执行安全编译结果；前后端质量门均通过。

文件预览边界：当前只执行同一 `fileVersionId` 内的单源计划，Join 仅支持等值条件，中间结果最多 200,000 行，并受文件大小、DSL 预览行数和 25 秒交互超时共同限制。大文件流式/分区执行、非等值 Join、多个文件版本或跨数据源计划归入 T0304，不能通过放宽内存上限绕过。

### T0303 实现安全查询编译器（已完成）

- [x] 实现参数绑定、只读校验、字段白名单和查询超时。
- [x] 注入租户、行级和列级权限。
- [x] 实现查询取消、结果行数限制和审计。

验收：DDL/DML、注释绕过、堆叠语句和越权字段测试全部失败关闭。

验收证据：`internal/querycompiler` 覆盖 MySQL/Oracle 方言、参数类型/多值、物理字段白名单、排序、行策略 AND/OR/DENY、列拒绝/脱敏/仅聚合与最小分组规模；恶意值只进入绑定参数。`internal/queryruntime` 覆盖数据库与固定文件版本成功执行、超时主动取消、用户取消和审计状态竞争。Connector 只读防线拒绝 DDL/DML、三类注释、危险函数、过程调用和堆叠语句；`test/sourceintegration` 已在真实 MySQL/Oracle 样例库执行编译结果。`platform.query_runs` 仅保存执行计划/参数哈希和运行指标并启用强制 RLS。

边界说明：平台租户隔离通过 RLS 解析租户内数据集、数据源、资产和策略，并绑定到该租户独立的数据源凭据；外部业务库没有统一 `tenant_id` 字段，因此编译器不会臆造源端租户列条件。若某个源表还包含业务租户列，必须通过数据集过滤或行级策略显式约束。

### T0304 实现跨源查询（实时预览第五阶段完成）

- [x] 支持 MySQL ↔ Oracle、数据库 ↔ Excel、Excel ↔ Excel 等值 Join，并允许设计器选择不同数据源资产建模。
- [x] 实现规范类型转换、可证明安全的单节点过滤下推和必要字段裁剪。
- [x] 实现受限实时预览模式；每源按预览行数放大取样但不超过 10,000 行，单数据集最多 16 个节点，任何源截断均失败关闭。
- [ ] 实现权限隔离的查询缓存模式。
- [ ] 实现可版本化、可原子切换的物化模式及刷新任务。
- [x] 保存执行计划摘要、参数摘要、节点子查询 ID、数据水位和来源版本；文件水位使用固定版本 SHA-256。
- [x] 按预览节点结果检测未确认关系、声明基数冲突、双侧重复键和多对多风险，将结构化中文告警返回设计器并持久化到查询审计。
- [x] 在 Join 前按实际有向执行顺序精确估算完整计划各阶段输出行数，任一阶段预计超过 200,000 行时在分配该中间结果前失败关闭。
- [x] 将可证明安全的 `SUM/MIN/MAX/COUNT/AVG(直接字段)` 预聚合下推到所有 Join 中均处于“多”侧的数据库节点，并由网关归并部分结果；COUNT 使用部分计数之和，AVG 使用 SUM/COUNT 加权结果，无法证明安全时自动回退原始扫描。
- [ ] 为 `COUNT(*)` 设计可审计的节点归属与外连接空侧语义，并为 `COUNT_DISTINCT` 明确精确集合或可接受误差的草图；当前两类聚合不下推。
- [ ] 为 DECIMAL 聚合引入精确十进制定点运算并统一日期/时间时区语义；当前跨源扫描会保留 64 位整数文本并拒绝溢出，但网关 SUM/AVG 最终仍使用浮点计算。
- [ ] 在 DSL 领域校验阶段拒绝“聚合输出与未分组明细字段混用”，并为 GroupBy/Having 建立完整聚合层级规则；当前预聚合计划器会安全回退，但单源数据库与文件执行器仍可能分别报错或采用首行语义。
- [x] 根据 Join 类型和数据量选择哈希构建侧；INNER Join 使用较小输入建表，外连接保持声明方向和补 NULL 语义。
- [x] 在 `query_run_sources` 保存可信网关采集的节点实际输入行数、读取及规范化耗时和状态；成功运行要求全部节点指标齐备，失败、超时和取消会收口残留 RUNNING 节点。
- [ ] 将预览期精确全计划估算升级为基于历史节点指标和元数据统计的轻量代价优化器；当前估算需要先加载受限节点样本，并额外传播最多 200,000 行必要 Join 键，不适合作为大数据物化计划器。
- [ ] 若后续引入分布式 Worker 或物理执行计划，补充广播/重分区 Exchange 选择；当前实时预览集中在单个网关进程执行，不存在可选择的跨 Worker 广播侧。
- [ ] 为 `query_run_sources` 增加不含源端敏感文本的节点错误码和执行阶段，区分连接、源查询、结果校验与规范化失败；当前仅持久化节点终态和运行指标。
- [ ] 持久化不含 SQL 的物理执行摘要与源端扫描指标，标明节点是否预聚合及数据库实际扫描行数；当前 `row_count` 只表示网关接收并校验的行数，启用预聚合时不能代表源表扫描量。
- [ ] 将当前进程内文件预览升级为流式、分区或物化执行，覆盖大文件与非等值 Join，并保持固定版本和权限语义。
- [x] 为数据库节点持久化稳定子查询 ID，支持多副本 API 跨实例调用 Connector 取消。
- [ ] 为文件节点增加共享查询归属/取消协调，避免文件查询只能由原 API 实例可靠取消。

验收：满足 `XD-01`、`XD-02`，结果可追溯到固定源版本。

阶段证据：`internal/federation` 并发执行正式节点扫描，在网关完成等值 Join、最终聚合、排序和行列策略；风险分析测试覆盖人工确认、ONE/MANY 声明冲突、双侧重复键、多对多、多阶段扇出、取消和 20 万行分配前阻断。INNER Join 会选择较小输入构建哈希索引，外连接保留声明语义。保守预聚合计划器仅向可证明安全的数据库“多”侧下推直接字段聚合，内部投影使用无碰撞别名并保持调用方 DSL 不变；测试覆盖最小分组权限、字段复用、ONE 侧、文件节点、COUNT(*) 和复杂参数回退，以及纯整数 SUM 溢出失败关闭。设计器已允许跨源选表、保存人工确认状态并显示风险提示。`test/sourceintegration` 用真实 MySQL/Oracle 样例库验证 Oracle 订单明细从 3 行预聚合为 2 行后，跨源归并得到相同区域收入、COUNT=2/1 和加权 AVG=750.375/800；`test/integration/dataset_test.go` 验证来源水位、取消标识、`warnings_json` 和成功/失败节点指标审计。缓存、物化、COUNT(*)/去重部分状态、精确 DECIMAL、统计型代价优化器及大文件执行尚未完成，因此 T0304 仍按部分完成统计。

### T0305 实现自定义 SQL

- [ ] 支持 MySQL/Oracle 源端只读 SQL。
- [ ] 支持联邦 SQL 或等价高级查询表达。
- [ ] 使用 AST 校验、参数绑定、权限重写和执行限额。

验收：自定义 SQL 不可绕过对象、行级和列级权限。

### T0306 实现指标与维度管理

- [ ] 实现指标表达式、聚合、单位、时间粒度和适用维度。
- [ ] 实现试算、草稿、发布、废弃和版本引用。
- [ ] 实现可加性、Join 扇出和引用循环校验。
- [ ] 为 KPI、图表和结论解析不可变指标版本，校验指标所属数据集版本、可用维度及语义字段兼容性；替换 T0407 仅负责筛选映射、不能执行示例 `metricId` 的过渡边界。

验收：满足 `MET-01`，发布报告引用不可变指标版本。

### T0307 实现血缘与影响分析

- [ ] 建立源文件/源表 → 字段 → 数据集 → 指标 → 组件 → 报告版本血缘。
- [ ] 元数据变化后生成受影响对象清单。
- [ ] 为高风险变化阻止发布或要求确认。

验收：删除或改型字段时能定位所有受影响报告组件。

---

## 8. P3：报告 JSON、画板与组件

### T0401 定义报告 JSON Schema V1（已完成）

- [x] 覆盖报告、画布、页面、分块、组件、参数、样式、绑定、冻结和权限引用。
- [x] 明确 1920×1080 是首屏基准，12×10 是首屏网格，纵向行数按 `max(10, block.y + block.h)` 动态增长。
- [x] 提供 0.9 旧画布字段迁移、严格未知字段拒绝、JSON Schema、Go 领域校验器和完整可执行示例。
- [x] 校验报告内 ID/编码唯一性、页面顺序、分块/组件越界与碰撞、4 倍内网格、冻结范围、权限角色编码和来源追踪枚举，并返回精确错误路径。

验收：有效示例全部通过，无效坐标、碰撞和未知组件能给出精确错误路径。

验收证据：`internal/reportjson` 与 `api/schemas/report-json-v1.schema.json` 为正式合同，覆盖确定性规范化和 SHA-256 哈希；单元测试覆盖完整样例、重复规范化、0.9 的 `logicalHeight/gridRows` 迁移、未知字段、内网格错误、组件碰撞、动态内容高度和冻结配置。原 `internal/spike/reportjson` 保持技术原型边界。

### T0402 实现统一页面渲染器（已完成）

- [x] 仅依赖版本化报告 JSON 和运行上下文还原页面，运行参数、组件状态和权限/角色快照不写回报告定义。
- [x] 设计器预览与在线查看共用 `ReportRenderer` 核心，并以兼容样例断言两种模式还原相同组件 ID 集合。
- [x] 实现组件注册、未知组件安全降级和单组件 React 错误边界；结构错误则显示合同路径并停止整页渲染。
- [x] 将 AJV 8.18.0 与 `ajv-formats` 作为直接依赖接入 Draft 2020-12 正式 Schema，共用仓库根目录样例，并通过依赖漏洞审计。
- [x] 分块和组件可见性同时检查 `requiredPermission` 与 `allowedRoleCodes`；渲染器不执行报告 JSON 中的任意 HTML 或脚本。

验收：相同 JSON 在设计器预览和查看器中的结构一致。

验收证据：`web/src/components/report` 提供正式渲染入口、注册表与错误边界，`web/src/lib/report-schema.ts` 提供 Draft 2020-12 校验及中文错误路径；前端测试覆盖有效样例、损坏画布、未知组件、设计/查看组件树一致、局部异常隔离、合同失败和角色拒绝。浏览器使用真实本地登录分别检查设计器与查看器，标题、图表、结论和来源均可见且控制台无错误。自适应缩放与长画板交互已在 T0403 接入同一渲染核心。

### T0403 实现长画板和主网格（已完成）

- [x] 固定 12 列、首屏 10 行，纵向内容行按分块最大 `y+h` 增长。
- [x] 实现鼠标/触控拖拽、缩放、网格吸附、键盘对齐、碰撞和越界校验。
- [x] 按容器宽度只缩小不放大，禁止横向溢出并保持纵向自然滚动。
- [x] 提供多个冻结项的顶部偏移堆叠和容器停止边界辅助算法；T0408 已在此基础上接入滚动态、缩放感知和父子冻结完整规划器。

验收：分块可移动到第 10 行以后，刷新后坐标和高度不变。

验收证据：`web/src/lib/report-layout.ts` 提供动态内容高度、整数网格吸附、不可变坐标更新、1000 行编辑态保护、碰撞/越界判断、容器缩放和初版冻结堆叠辅助；完整浏览态冻结规划位于 T0408 的 `report-sticky.ts`。设计器支持指针拖拽、缩放手柄、方向键移动及 `Shift + 方向键` 缩放；T0403 当时使用按报告 ID 隔离的浏览器会话草稿完成刷新验收，该临时事实源已由 T0409 服务端草稿替换。测试覆盖长页面、碰撞、边界、吸附、缩放、冻结范围和刷新恢复。真实浏览器在 624 px 内容容器下测得缩放比 0.325、画布与容器同宽且无横向溢出，并完成键盘碰撞拒绝和真实指针拖拽；打印样式强制冻结项回到普通文档流。

### T0404 实现分块内动态网格（已完成）

- [x] `M×N` 分块生成 `4M×4N` 内网格。
- [x] 支持在分块内拖入、移动、缩放、复制和删除组件，同时提供方向键、快捷键和“选择后点击空白处”的键盘/触屏替代交互。
- [x] 分块缩放后按比例重算内网格和组件坐标，自动寻找无碰撞位置；新空间无法容纳全部组件时拒绝缩放，不静默丢失内容。

验收：满足 `CAN-02`、`CAN-04`。

验收证据：`web/src/lib/report-layout.ts` 统一实现内网格派生、有限数值防护、组件新增/移动/缩放/复制/删除、报告内唯一标识、锁定/碰撞/越界判断和确定性空位搜索；`ReportRenderer` 与 `ReportDesignerCanvas` 接入指针、键盘、原生拖放及点击插入交互。测试覆盖分块缩放回流、组件边界与锁定、拖入、移动、复制、删除和触屏替代路径。真实浏览器在 0.325 缩放画布中完成删除、新增指标卡、方向键移动、`Shift` 缩放、快捷键复制及刷新恢复，最终 5 个组件、无横向溢出和布局告警。

### T0405 实现分块清空、删除和 1×1 恢复（已完成）

- [x] 实现“清空分块”和“删除分块”两个操作，并以不同历史操作类型保留语义。
- [x] 操作后将原 `M×N` 区域恢复为 `M×N` 个隐式空白 `1×1` 基础单元；单元选中或放入组件时才创建正式分块 JSON。
- [x] 非空操作使用可访问页面对话框二次确认并展示组件数量，支持取消、Escape 和焦点循环；空分块不重复确认。
- [x] 支持统一撤销/重做、100 项历史上限、`Ctrl/Cmd + Z`、`Ctrl/Cmd + Shift + Z`、锁定和前端编辑权限校验；历史栈覆盖 T0404/T0405 全部布局操作。

验收：满足 `CAN-05`，画板不出现不可选择的空洞。

验收证据：`resetBlock` 释放正式分块并返回原网格，`deriveEmptyGridCells` 派生未占用基础单元，`createBlockAtCell/createBlockWithComponent` 在用户实际操作时原子创建 `1×1` 分块；编辑态最小行数保存在独立 `editorState` 中并随 T0409 服务端草稿持久化，不污染正式报告 JSON。8 个前端测试文件共 51 项用例覆盖确认/取消、锁定、只读权限、基础单元、组件落入、统一历史、快捷键和刷新恢复。真实浏览器验证确认框显示 1 个组件，清空后恢复 48 个空白单元，撤销/重做正确，选中单元生成正式分块且刷新后保持 14 行；最终无告警和横向溢出。

### T0406 实现组件库第一批（已完成）

- [x] 标题、结构化富文本、筛选器、指标卡、附加信息卡。
- [x] 表格、柱状图、条形图、折线图、饼图。
- [x] 图片、附件、数据来源、更新时间和结论组件。
- [x] 每类组件提供配置 Schema、默认值、最小尺寸和统一加载/空值/错误态。
- [x] 用正式组件目录替换 T0404 临时工厂尺寸，并让设计器面板、默认配置和渲染器从目录派生；类型检查阻止目录项遗漏渲染器。
- [x] 将报告 V1 中开放的 `style/binding/interaction/refreshPolicy` 收紧为按组件类型的条件 Schema，并在 Go 校验器中校验结论引用的已解析指标和图表组件真实存在。

验收：所有组件可从 JSON 创建、编辑、复制、删除和重新加载。

验收证据：`report-component-catalog.ts` 注册 12 种正式组件类型（图表类型覆盖柱、条、线、饼），提供默认配置、默认尺寸和不超过 `4×4` 的最小尺寸；设计器面板、拖放解析和组件工厂从目录派生。JSON Schema 拒绝未知配置、非法图表类型、组件专属刷新策略错误和小于正式最小尺寸的网格；Go 校验器独立拒绝未知配置键、非法图表类型和刷新策略，并在所有页面收集完成后校验结论的指标及图表引用。10 个前端测试文件共 64 项用例覆盖目录一致性、全部默认组件序列化重载、四类图表、加载/异常态、危险资源协议、配置拒绝和最小尺寸；Go 测试覆盖服务端配置与引用语义。真实浏览器确认设计器展示 12 个正式面板项，结构化富文本可从空白单元创建并在刷新后恢复，624 px 画布容器无横向溢出；查看器标题、趋势图、结论和来源正常，控制台无错误。

### T0407 实现筛选、联动和数据绑定（已完成）

- [x] 筛选器通过 `parameterId` 绑定报告参数，并由 `semanticBinding` 将一个报告语义字段显式映射到各数据集版本自己的 `fieldId` 和 `datasetParameterCode`；运行参数统一使用 `Parameter.code`。
- [x] 支持报告、页面、分块和指定组件作用域，拒绝未知/重复目标、缺失/歧义数据集映射及页面参数跨页影响。
- [x] 支持使用机器可读语义点值的图表点击联动、声明式下钻、参数刷新状态及按组件 latest-wins 乱序响应保护；重叠目标只接受最新代次，不相交目标独立完成，交互运行态不写回报告 JSON。

验收：跨数据集筛选通过语义字段映射正确生效。

验收证据：`api/schemas/report-json-v1.schema.json`、`internal/reportjson` 和 `web/src/lib/report-contract.ts` 定义参数语义映射、四种作用域、图表 linkage/drill 及引用校验；`web/src/lib/report-interactions.ts` 与 `internal/reportbinding` 分别提供前后端无 IO 纯解析器，把 JSON 参数 ID 确定性转换为运行参数 code 和组件目标。正式样例为同一参数配置筛选、图表联动和下钻，`demo-report-runtime.ts` 提供可替换的示例执行器。115 项前端单测覆盖参数类型、四种作用域、两个独立数据集使用不同字段/参数编码、操作符基数、歧义标识、深拷贝、机器可读图表点、多级返回、按组件 latest-wins、严格模式忙碌态、运行实例隔离及失败后恢复；Go 单测独立覆盖相同跨数据集映射、操作符基数、图表维度白名单、失败关闭和输入不可变性。真实浏览器验证月份筛选、图表点击、季度→月份下钻与两级返回均同步更新确定状态，交互按钮可聚焦、选中点具有 `aria-pressed`，1280 px 视口无横向溢出且控制台无错误。精确已发布数据集版本查询与 `ONLINE` 执行仍归入 T0601/T0602，完整可访问交互浏览器矩阵归入 T0801。

### T0408 实现浏览态冻结（核心实现已完成，完整浏览器矩阵见 T0801）

- [x] 支持分块和组件的 sticky 开关、顶部偏移、作用域和层级。
- [x] 多冻结项自动堆叠，离开约束容器后停止悬浮。
- [x] 统一 Schema、Go 和 TypeScript 冻结判别合同；禁用态只保留 `enabled=false`，启用态校验完整字段、数值上限及分块/组件各自允许的作用域。
- [x] 明确 `CONTAINER` 的 V1 祖先语义：分块只能引用所属页面，组件只能引用所属页面或分块；未知、跨页、自引用歧义或非祖先引用失败关闭，且不得把 `containerId` 当作 DOM 选择器。
- [x] 明确 `sticky.top` 为视口 CSS 像素并按画布缩放换算；只让实际渲染且有权限的可见项参与堆叠，视口、容器、运行提示或尺寸变化时批量重算。
- [x] 固化父分块与子组件同时冻结、横向不相交项、相同层级和容器容量不足时的确定性顺序与普通文档流降级语义。
- [x] PDF/打印模式恢复普通文档流。
- [x] 窄屏/触控或冻结组空间不足时恢复普通文档流；降级后正文、键盘焦点和关键操作不得被仍冻结项遮挡。

验收：满足 `CAN-03`，滚动、缩放和多冻结项场景通过视觉测试；完整设备、缩放、父子、边界和打印矩阵在 T0801 关闭。

验收证据：报告 Schema 使用严格冻结判别分支，Go 模型以存在性感知解码拒绝缺失、空对象、`null` 和禁用夹带参数，同时保留 0.9 迁移兼容；Go、AJV 和 TypeScript 统一 `top 0..10000`、`zIndex 1..100000` 及 V1 同页祖先规则。`report-sticky.ts` 在逻辑坐标中实现缩放换算、横向相交堆叠、页面/分块底边、容量不足失败关闭、父子位移扣除和跨类型同 ID 隔离；共享查看器只让实际可见且有权限的项参与，以动画帧合并滚动，并监听画布、父容器和滚动容器尺寸变化。待命项不提前建立层叠组，子组件层级不再抬高整个父分块；查看态始终保留等高降级状态槽，避免告警改变自身规划输入或覆盖正文。设计器受控面板覆盖目标选择、开关、作用域、容器、偏移和层级，尊重配置锁并进入统一历史；T0408 当时的会话草稿已由 T0409 服务端草稿和 Patch 队列替换。正式样例启用并排标题与筛选器。前端 12 个测试文件共 137 项、Go race/coverage、19 项 Connector、数据库验证、PostgreSQL 集成和真实 MySQL/Oracle 来源集成均通过。真实浏览器冒烟在 1280×720、画布缩放 0.5865 下确认空状态槽恒为 54px、待命项无 transform/zIndex，滚动后并排两冻结项才写入独立层级并稳定贴顶；冻结筛选器联动后画布与标题顶部保持不变、无降级告警且操作正常。浏览器也复现并修复了运行提示插入导致画布下移却未重测的 46px 偏差。纵向堆叠、容器底边、父子位移、容量不足和跨作用域层级由确定性单元/渲染回归覆盖，尚未宣称完成真实浏览器全矩阵。打印和触控媒体规则会清除位移、层级与宿主溢出；正式导出 Worker 稳定信号、全矩阵可访问回归和滚动性能预算已分别回补到 T0703、T0801、T0803。

### T0409 实现报告草稿 JSON 持久化与审计（核心已完成）

- [x] 新增 `reports`、`report_drafts`、不可变 `report_revisions`、操作者绑定的 `report_idempotency_records`、可重建组件/依赖索引及 `report_edit_guards`；全部使用租户复合外键与强制 RLS。服务端 UUID 同时作为数据库、对象权限和持久化 `report.id`，保存接口只接受 `DRAFT`。
- [x] 提供创建、加载、批量语义保存和有界修订查询接口。常规保存提交 `expectedRevision`、有序 `changes[].patch`、结果完整 JSON 与独立编辑器状态；服务端逐 change 应用、重新 `Prepare`，最终规范 JSON/哈希不一致则整批回滚。
- [x] Patch 仅允许 `add/remove/replace`，拒绝根替换、未知字段、非法 `~0/~1`、歧义/越界数组下标及无实际变化；请求信封 ≤3 MiB、规范 JSON ≤2 MiB、Patch ≤256 KiB、每请求 change 与原子操作均 ≤100、指针 ≤2048 字节/64 段。结构数量与资源耗尽上限继续归入 T0802。
- [x] 使用稳定语义类型校验分块/组件移动、缩放、创建、清空、删除、冻结、复制及撤销/重做的真实前后差异和 target；复制同时记录原组件与新组件且强制不同 ID，通用 `DOCUMENT_UPDATE` 不能绕过语义校验。`UNDO/REDO` 还要求整份规范哈希精确回到被引用修订的 before/after，拒绝把同实体另一项合法修改伪装成补偿操作。
- [x] 读取和修订查询在仓储租户事务中校验当前 `REPORT/READ`，更新及幂等重放重新校验当前 `REPORT/UPDATE`，并检查旧草稿三类分块锁、组件 `manualLocked`、乐观锁版本和活跃报告/分块占用；同批先解锁再修改仍以请求开始时文档拒绝。即使只有全局 CREATE 权限的创建者也会获得自己新报告的对象级 READ/UPDATE，GET 返回的 `capabilities.edit` 只用于界面收口。
- [x] POST/PUT 均强制 1..128 字节 `Idempotency-Key`；租户、入口、报告、可信操作者、规范请求哈希和首次完整响应绑定，同键同载荷在当前权限校验后精确重放且先于陈旧版本判断，同键异载荷或跨操作者重放冲突。
- [x] 草稿行锁、修订、完整 JSON/哈希、编辑器状态、组件/依赖索引、通用审计和幂等响应在一个租户事务中提交；操作者、时间、来源与 Patch 哈希由服务端生成，派生索引不接受客户端写入。
- [x] 前端只保留一份当前完整 JSON，最近 100 项历史改存正/逆 Patch；一次 pointerdown→pointerup 只提交最终手势，撤销/重做生成独立待审计操作。每份草稿只有一个在途保存，旧响应仅确认其提交的 operation ID，保存期间新增修改继续保持 DIRTY；首次创建前的编辑聚合进入 `REPORT_CREATE`，取得服务端 UUID 后才逐操作修订。
- [x] 设计器从服务端加载已有 UUID 草稿，展示加载/未保存/保存中/已保存/冲突/失败/只读状态；未持久化模板初始即为未保存，409 导出使用最新本地引用且不自动覆盖，403 切换只读；普通站内链接和页面卸载对未保存/在途修改给出离开确认。
- [x] 对已有 UUID 报告精确迁移 `report-layout-draft:{reportId}`：先加载服务端再验证 `report-designer-session-v1` 信封或更早期裸 V1 文档、报告身份、Schema 和 `minimumRowsByPage`，相同或损坏副本只清理精确键，差异副本需用户确认并以 `LEGACY_DRAFT_RECOVERY` 保存成功后才删除；认证会话键不受影响。
- [x] PostgreSQL 集成覆盖创建者对象授权、跨租户隔离、连续保存、陈旧冲突、跨操作者幂等拒绝、权限撤销后禁止重放、补偿哈希精确互逆、后续修订后的原响应重放、派生索引与任务占用；数据库验证覆盖报告 RLS、修订不可变性和基础一致性约束，前端覆盖在途继续编辑、冲突最新快照、新建未保存状态及旧草稿精确迁移。
- [ ] 将当前显式“创建/保存草稿”扩展为可配置防抖自动保存，复用单在途候选与原幂等键重试，并补齐离线、指数退避、页面隐藏、连续编辑和自动保存 P95 验收；自动保存不能清除请求发出后的新修改。
- [ ] 将通用 `LEGACY_DRAFT_RECOVERY` 收敛为独立迁移端点或服务端签发的一次性恢复凭据，并记录报告级迁移状态；当前入口仍要求 UPDATE、版本、旧锁、完整合同和显式用户确认，但授权客户端可重复使用宽范围恢复语义。
- [ ] 补齐 `/designer/draft|new|demo` 旧键在首次创建前的显式迁移选择、浏览器前进/后退与非链接式站内跳转阻断，以及冲突/旧草稿的结构化差异视图；当前已有窗口卸载、普通站内链接、导出和远端重载保护。
- [ ] 按 T0803 压测结果实现服务端 Patch 正文压缩/保留期、快照间隔和前端设计器懒加载；不可变修订信封、基础/结果哈希与通用审计不能随压缩丢失。

验收证据：双租户 PostgreSQL 测试仅凭服务端规范草稿重新加载并验证 RLS；服务端逐 change 重放与客户端完整定义得到相同规范哈希，锁/身份/状态/超限/语义不符均失败关闭。迁移 `000020`-`000023` 建立草稿、显式旧草稿恢复、操作者绑定幂等合同和不可变修订一致性约束。前端 15 个测试文件共 175 项覆盖 Patch 往返、100 项历史、手势收敛、服务端加载/保存、在途编辑、冲突最新快照、服务端只读能力、新建未保存保护及旧键精确清理；真实浏览器完成本地登录、创建服务端草稿、修改冻结配置、保存和刷新恢复，刷新后保持已保存与冻结启用状态且控制台无错误。不可变发布版本、对象存储制品和在线加载仍明确归入 T0601/T0604。

---

## 9. P4：AI 报告与分块 AI 浮窗

### T0501 实现 AI 编排基础设施

- [ ] 实现文本/视觉模型 Provider Adapter。
- [ ] 实现超时、重试、限额、成本统计和租户级授权。
- [ ] 实现输入脱敏、最小化和完整 AI 审计。

验收：供应商切换不影响报告领域接口；禁止发送凭证和未授权字段。

### T0502 实现两阶段报告生成

- [ ] 接收需求描述、附件、图片和授权资源范围。
- [ ] AI 先生成语义草稿 JSON。
- [ ] 服务端解析真实数据集、指标、字段和组件引用。
- [ ] 未解析资源明确标记，不允许静默执行。

验收：满足 `GEN-01`、`GEN-02`。

### T0503 实现分块 AI 浮窗交互

- [ ] 鼠标悬停或选中分块时显示 AI 入口；触屏使用选中模式。
- [ ] 支持自然语言输入、快捷指令、处理中、失败和取消状态。
- [ ] 浮窗不遮挡分块关键操作，键盘可访问。

验收：任意可编辑分块均可唤起，锁定或无权限分块不能发起修改。

### T0504 实现分块级 AI 上下文与 JSON Patch

- [ ] 默认仅发送当前分块 JSON、主题和授权语义资源。
- [ ] AI 只能返回当前分块路径内的 JSON Patch。
- [ ] 校验 Schema、权限、绑定、碰撞、尺寸和人工锁定状态。
- [ ] 展示变更前后预览，确认后应用，支持撤销和审计。
- [ ] 跨分块修改必须由用户明确启用并再次确认。

验收：满足 `CAN-06`，恶意或越界 Patch 被服务端拒绝。

### T0505 实现 AI 结论生成

- [ ] 结论只使用绑定指标、维度结果和数据快照。
- [ ] 保存证据引用、模型版本和生成时间。
- [ ] 支持人工修订、锁定和重新生成差异预览。

验收：结论中的数字均可追溯，人工锁定内容不会被覆盖。

---

## 10. P5：发布、在线运行与公开访问

### T0601 实现报告版本与发布校验

- [ ] 区分报告定义、不可变报告版本和报告运行实例。
- [ ] 从指定且未变化的草稿版本生成新的不可变 `report_version`：服务端把状态收敛为 `PUBLISHED`，生成规范 JSON、SHA-256、Schema 版本、字节数和只写一次的报告对象存储地址，并原子更新当前已发布版本指针；禁止覆盖或原地修改既有版本及其对象。
- [ ] 报告制品只能由服务端写入独立 reports 桶，对象键包含租户、报告、版本/制品 UUID 与内容哈希；数据库保存版本元数据和地址，不把另一份可变 JSON 当作发布事实来源。写对象、登记版本或更新指针失败时必须可幂等重试并收口孤儿对象。
- [ ] 发布前校验 JSON、依赖版本、权限、查询、附件、冻结和布局。
- [ ] 发布时由服务端重新解析冻结配置，校验 scope/containerId 祖先引用、top/zIndex 上限、同页约束和静态堆叠可容纳性；失败返回页/分块/组件级路径并阻止发布。
- [ ] 发布时解析并固定每个组件及语义映射引用的已发布 `datasetVersionId` 和不可变指标版本，拒绝草稿、失效、跨租户或与字段/参数不一致的依赖；不能把当前数据集草稿指针当成发布事实。
- [ ] 发布失败返回组件级错误路径。
- [ ] 发布任务持有版本引用或编辑租约时，将占用状态返回设计器并阻止清空/删除关联分块；任务结束或租约过期后自动释放。

验收：草稿修改不会改变已发布版本。

### T0602 实现报告运行服务

- [ ] 解析组件依赖并合并重复查询。
- [ ] 注入权限快照、参数和数据水位。
- [ ] 支持 `ONLINE/SCHEDULED/EXPORT/PREVIEW` 运行类型。
- [ ] 提供按精确已发布 `datasetVersionId` 执行的 `ONLINE` 查询入口，复用现有单源、文件和跨源安全运行链路；不得通过 datasetId 读取可变的当前草稿。
- [ ] 在服务端统一解析报告参数类型、系统默认值和报告时区，把 `DATE_RANGE`、年/月/季度、选择类及系统参数安全转换为各数据集声明的基础参数类型。
- [ ] 提供权限约束的动态选项与级联选项协议，定义去重、搜索、分页、最多选项数和单次返回上限；静态和动态选项使用相同规范值，不能用展示标签反推查询值。
- [ ] 对一次交互触发的多目标查询实现主动取消、服务端代次/乱序保护、组件级部分失败、有限重试和明确的不可重试错误；前端 latest-wins 不能替代服务端资源收口。
- [ ] 单组件失败不导致整页白屏。
- [ ] 定义与前端 `ReportRuntimeContext` 对齐的版本化运行响应，明确组件 `READY/LOADING/ERROR`、更新时间、脱敏错误码、权限/角色快照和部分成功语义，禁止运行数据反向修改报告 JSON。
- [ ] 为每类组件定义正式运行时负载 Schema、统一错误码和行/点/附件数量上限；替换 T0406 表格 20 行展示保护，并明确分页、数字/日期格式化和空值协议。

验收：运行记录可追溯到报告、数据集、指标和 Excel 文件固定版本。

### T0603 实现缓存和物化

- [ ] 缓存键包含租户、权限版本、参数、数据集版本和数据水位。
- [ ] 支持查询缓存、物化数据集和整份报告快照。
- [ ] 实现失效、刷新、容量和租户配额策略。

验收：不存在跨租户或跨权限缓存污染。

### T0604 实现在线查看器

- [ ] 支持长页面、筛选、联动、下钻、刷新和冻结。
- [ ] 支持加载、空数据、部分失败和权限不足状态。
- [ ] 展示数据更新时间、来源和报告版本。
- [ ] 页面切换、全屏/嵌入容器变化、权限显隐和运行实例切换后重建冻结栈并释放旧观察器与监听器，禁止残留跨页或跨实例冻结状态。
- [ ] 若产品要求页面作用域组件无条件跨越父分块的显式层叠组比较 `zIndex`，实现保持单一 DOM、焦点和屏幕阅读器顺序的页面级提升层；V1 先采用 CSS 原生父组边界，禁止用复制组件节点伪造全局层级。

验收：满足 `RUN-01`，设计器预览与在线查看视觉一致。

### T0605 实现公开访问

- [ ] 生成不可猜测公开地址，支持访问码、有效期和禁止下载。
- [ ] 公开运行使用脱敏快照或公开专用权限身份。
- [ ] 撤销公开后立即失效地址和缓存。
- [ ] 增加限流、防枚举、安全响应头和访问审计。

验收：满足 `PUB-01`，匿名访问无法横向读取其他报告或后台资源。

---

## 11. P6：调度、邮件与 PDF

### T0701 实现持久化任务框架

- [ ] 实现任务创建、领取、心跳、重试、取消、死信和幂等键。
- [ ] 按租户限制并发和资源占用。
- [ ] 提供任务状态、错误和耗时查询。

验收：Worker 重启后任务不丢失、不重复产生不可逆副作用。

### T0702 实现报告调度

- [ ] 支持 Cron、时区、参数模板、启停和补跑。
- [ ] 调度固定引用报告版本或明确的“最新已发布版本”策略。
- [ ] 记录计划时间、实际时间和数据水位。

验收：满足 `SCH-01`，跨时区和错过执行窗口行为明确。

### T0703 实现 PDF 渲染与归档

- [ ] 使用受控浏览器加载同一报告 JSON 和运行快照。
- [ ] 处理长页面分页、图表完成信号、字体、页码和水印。
- [ ] 导出 Worker 显式切换打印渲染模式，并等待“冻结已降级、布局已稳定”信号后再分页；固定样例断言无冻结残留和分页遮挡。
- [ ] 保存 PDF 哈希、对象地址、报告版本和数据快照引用。

验收：满足 `PDF-01`，相同版本和快照可复现一致 PDF。

### T0704 实现邮件发送

- [ ] 支持摘要、PDF 附件和受控链接。
- [ ] 发送前校验收件人授权和公开策略。
- [ ] 实现重试、退信记录和敏感信息保护。

验收：满足 `MAIL-01`，不同权限收件人不会共享错误附件。

### T0705 实现历史版本与归档中心

- [ ] 查看报告版本、运行参数、数据水位、结论修订和归档文件。
- [ ] 支持权限内下载、过期和保留策略。

验收：满足 `HIST-01`，任一归档可完整追溯生成依据。

---

## 12. P7：质量、安全与生产就绪

### T0801 完成自动化测试矩阵

- [ ] 单元测试覆盖核心领域规则和编译器。
- [ ] 集成测试覆盖 PostgreSQL、MySQL、Oracle、Excel、缓存和对象存储。
- [ ] 端到端测试覆盖登录、建模、设计、AI 修改、发布、公开、PDF 和邮件。
- [ ] 建立报告 JSON 与数据集 DSL 兼容性样本库。
- [ ] 固化 T0401 初始 V1 与 T0406 收紧后 V1 的兼容样本、迁移预期和注册表/Schema 一致性检查；发布后不得在同一版本号下继续引入破坏性约束。
- [ ] 为设计器建立真实浏览器矩阵，覆盖原生拖放、指针捕获、触屏/键盘替代操作、不同缩放比、刷新恢复和撤销/重做；JSDOM 单测不能替代这些交互验收。
- [ ] 为筛选、图表联动和下钻建立真实浏览器矩阵，固化机器可读 `points[].semanticValues` 协议，覆盖键盘焦点、可访问名称、`aria-pressed`、加载禁用、返回上级及快速连续点击；展示标签不得成为查询值。
- [ ] 增加分块重置对话框的焦点进入/循环/恢复、Escape、屏幕阅读器名称和移动端视觉回归测试；组件铺满分块时也必须能通过 `focus-within` 操作分块工具栏。
- [ ] 为浏览态冻结建立真实浏览器矩阵，覆盖桌面/窄屏、`1/0.5/0.325` 缩放、嵌入滚动容器、动态高度、页面切换、纵向堆叠、容器底边、父子冻结、显式父层叠组、权限显隐及打印；DOM/屏幕阅读器顺序保持唯一，降级时焦点不丢失。

### T0802 完成安全测试

- [ ] 租户越权、对象越权、行列权限和缓存污染测试。
- [ ] SQL 注入、文件上传、公式注入、路径穿越和恶意压缩文件测试。
- [ ] 公开链接枚举、访问码暴力破解和撤销失效测试。
- [ ] AI Prompt Injection、越权资源引用和越界 JSON Patch 测试。
- [ ] 对 T0409 Patch 解析和保存做属性/模糊测试，覆盖非法 `~0/~1` 转义、空/根指针、数组负数/前导零/越界下标、非末尾 `-`、禁用操作、过深路径、重复或相互依赖操作、规范完整 JSON 不一致、同批解锁后修改、身份/状态改写、同幂等键异载荷及部分重放失败；任一失败不得留下半份草稿、索引或审计。
- [ ] 补齐 T0409 HTTP 与 PostgreSQL 并发矩阵：相同/不同幂等键并发创建和保存、同一 `expectedRevision` 竞争、跨租户 Update/List/Replay、同租户无对象权限读取、请求处理中撤权、真实补偿引用、中批次失败全事务回滚，以及未知字段、尾随 JSON 和 3 MiB 请求体边界。
- [ ] 在 Go 校验器、JSON Schema、草稿保存和前端渲染入口统一限制页面数、单页分块数、单分块组件数、全报告组件数和最大内容行数，补充超大但结构合法 JSON 的服务端 CPU/内存与浏览器资源耗尽测试；T0409 的字节、Patch 数量和指针上限不能替代结构限额。
- [ ] 伪造前端 `report:edit`、`capabilities.edit`、旧 `sessionStorage` 草稿、锁定字段、人工锁、历史快照、补偿操作引用、旧草稿恢复语义或客户端结果完整 JSON，不能绕过服务端 `REPORT/UPDATE` 对象权限、版本冲突和 `report_edit_guards` 占用；对已实现的 `UNDO/REDO` 整份规范哈希精确互逆证明补充属性测试，并覆盖一次性旧草稿恢复凭据；发布任务占用与租约绕过测试在 T0601 实现后接入。
- [ ] 服务端必须依据已发布报告重新计算筛选/联动作用域、组件目标和语义字段映射，拒绝客户端伪造的目标组件、数据集版本、字段、参数编码、点击维度和跨页作用域；审计只保存必要 ID、计划/参数摘要和脱敏错误，不记录筛选值、图表点值或业务数据样本。
- [ ] 为图片和附件建立租户域名白名单、对象存储短期签名、MIME 嗅探、下载权限、CSP 与 `data:`/`blob:` 使用策略；验证结构化富文本不能注入 HTML、脚本或危险链接。

### T0803 完成性能压测

- [ ] 测试已缓存报告首屏 P95。
- [ ] 测试长画板、大量组件和多冻结项渲染。
- [ ] 测试万级数据、跨源 Join、Excel 大文件和并发导出。
- [ ] 输出容量模型和租户默认配额。
- [ ] 对最多 12000 个隐式基础单元采用视口虚拟化或区域合并渲染，并评估“单份当前 JSON + 100 条正逆 Patch”历史的内存；T0409 已移除 100 份完整快照，但长画板 DOM、Patch 值和撤销队列仍可能明显占用内存。
- [ ] 量化 Draft 2020-12 校验器、冻结运行时和 T0409 草稿客户端对首屏 JS 体积及解析耗时，必要时采用 standalone 预编译、独立分包或设计器懒加载；T0409 核心完成后生产主包约 524 KB（gzip 约 158 KB），已触发 500 KB 构建警告。
- [ ] 为长表格接入分页或行虚拟化，为大点数图表接入抽样/降采样，并对不可见长画板组件采用延迟渲染；T0406 首批渲染器只适用于受控运行时负载。
- [ ] 压测快速连续筛选、图表点击和下钻产生的请求风暴，覆盖多数据集目标数、旧请求取消耗时、latest-wins 丢弃率、部分失败重试、浏览器主线程占用和运行服务并发上限。
- [ ] 为多冻结项滚动建立 FPS/INP、布局重算次数和 CLS 预算；尺寸监听与边界测量必须批处理，避免每次 scroll 同步测量或触发整棵报告 React 更新。
- [ ] 压测 T0409 的完整 JSON 规范化、Patch 生成/逐操作重放、哈希、派生索引重建和操作日志查询，给出草稿自动保存 P95、单租户并发、历史容量、快照间隔与 Patch 正文压缩阈值；同时定义报告软删除后的审计保留期、最终清理/匿名化和墓碑策略，压缩或清理不得破坏基础/结果哈希链及不可变审计信封。
- [ ] 对连续拖拽和缩放建立手势级性能预算：高频指针移动只更新本地预览，释放时最多生成一个历史项和一批保存请求；测量在途保存期间继续编辑、连续手势排队、冲突重载和导航阻断，禁止出现逐帧 Patch/请求风暴、主线程长任务或误报已保存。

验收：缓存/物化命中时已发布报告首屏 P95 ≤ 1 秒，或记录经过批准的例外。

### T0804 建立可观测性

- [ ] 统一日志、指标和追踪关联请求、查询、AI、任务和报告运行。
- [ ] 建立错误率、延迟、连接池、缓存、任务积压、模型成本和组件失败率告警。
- [ ] 日志自动脱敏，不输出连接串、密码、Token 和敏感数据样本。

### T0805 完成备份、恢复与发布方案

- [ ] 备份 PostgreSQL、对象存储 JSON/PDF/Excel、迁移记录和密钥引用元数据。
- [ ] 演练恢复、回滚和 Schema/JSON 版本迁移。
- [ ] 编写部署、升级、降级和故障处理手册。
- [ ] 明确报告 Schema 版本冻结、向前/向后兼容和迁移窗口；T0406 在首次发布前收紧了 `1.0`，生产发布后同类破坏性调整必须升级版本并提供迁移器。

### T0806 执行最终验收

- [ ] 逐项执行需求文档第 29 章验收标准。
- [ ] 对未通过项建立缺陷、责任任务和目标版本。
- [ ] 输出上线检查表、已知限制和后续迭代清单。

---

## 13. 首个可运行纵向切片

Codex 应优先完成以下最小链路，避免各模块长期处于不可集成状态：

1. `T0001` Go 工程；
2. `T0002` 前端工程；
3. `T0003` PostgreSQL/对象存储本地环境；
4. `T0101` 基础租户模型；
5. `T0204` Excel 数据源；
6. `T0301` 数据集 DSL；
7. `T0302` Excel 单源数据集；
8. `T0401` 报告 JSON Schema；
9. `T0402` 页面渲染器；
10. `T0403` 长画板；
11. `T0405` 分块清空/删除；
12. `T0406` 标题、表格、柱状图和结论四类组件；
13. `T0409` 报告草稿 JSON 持久化与审计；
14. `T0601` 不可变报告版本与 JSON 制品发布；
15. `T0604` 在线查看器。

该纵向切片完成后，用户应能：上传 Excel → 创建数据集 → 配置指标/图表 → 编辑长画板 → 清空或删除分块 → 保存报告 JSON → 发布 → 在线查看。

---

## 14. 暂不进入首个纵向切片的事项

以下能力保留在总体计划中，但不得阻塞首个可运行版本：

- 亿级明细实时分析；
- 多人实时协同编辑；
- 复杂 ETL 和数据仓库开发；
- 专业出版级自由排版；
- 大量高级图表和地图能力；
- 多模型自动路由优化；
- 除 MySQL、Oracle、Excel 之外的数据源；
- 移动端专用设计器。

---

## 15. Codex 任务交付模板

每完成一个任务，Codex 应在最终回复中使用以下结构：

```text
任务：Txxxx <任务名称>
状态：完成 / 部分完成 / 阻塞

完成内容：
- ...

主要文件：
- ...

验证结果：
- 命令：...
- 结果：...

数据库/API/配置变化：
- ...

遗留风险：
- ...

建议下一任务：
- Txxxx ...
```

> TODO 勾选必须与真实完成状态一致。未经测试或只创建空接口的任务不得标记为完成。

---

## 16. 2026-07-15 全链路审计新增回补任务

以下任务来自本次对需求、TODO、代码、迁移、测试和 Docker 运行状态的逐项复核。它们用于补足原计划中描述过于笼统或完成状态高于证据的问题，不替代 T0301 之后的主线开发。

### T0901 修复 Connector 本地开发与测试入口（P0）

- [ ] 在开发文档中加入 Python 3.13 虚拟环境和 `requirements-dev.txt` 安装步骤。
- [ ] 让 `make connector-test` 与 CI 共用锁定的 `requirements-dev.txt` 隔离环境或测试镜像，支持可配置 Python 解释器，并在缺少 `pytest`、`oracledb`、`PyMySQL` 时前置给出可操作提示；不得依赖开发机全局 Python。
- [ ] 在全新环境验证 Connector 的 19 项测试可由文档命令一次通过。

审计证据：CI 会先安装 Python 依赖，因此流水线定义可用；2026-07-16 直接运行 `make connector-test` 因全局 Python 缺少 `oracledb` 失败，在仓库外临时隔离目录安装锁定依赖后 19 项通过。本地统一入口仍待 T0901 收口，不能用一次性的环境补齐替代工程化测试入口。

### T0902 补齐真实数据库元数据端到端验收（P0）

- [ ] MySQL 覆盖连接测试、元数据同步、结构变化、删除失效、重新激活和只读查询。
- [ ] 固定 MySQL 客户端/表字符集并增加中文等非 ASCII 文本往返断言；当前本地样例查询出现乱码，跨源数值链路暂以稳定区域编码验收。
- [ ] Oracle 覆盖 Service Name、SID、Schema 过滤、元数据同步、类型映射、超时和取消。
- [ ] 测试必须经过 Go `Service` 和 Python Connector，并断言 PostgreSQL 中的快照、资产、结构哈希和差异记录。
- [ ] 将真实源测试加入 CI 的独立 Job；若 Oracle 启动时间不适合每次 PR，则至少建立可见的夜间流水线。

审计证据：现有 `test/sourceintegration` 验证了 MySQL/Oracle 查询和 Go Hash Join，但没有验证元数据同步落库及差异生命周期；当前 CI 也只启动 PostgreSQL、Redis、MinIO。

### T0903 补齐数据资产中心集成测试（P0）

- [ ] 覆盖表/字段业务元数据更新、乐观锁冲突和审计日志。
- [ ] 验证技术元数据刷新不会覆盖人工业务描述、标签、敏感级别、语义类型和锁定状态。
- [ ] 覆盖租户目录公开、搜索筛选、差异列表和下游影响分析。
- [ ] 覆盖资产管理 HTTP API 的允许、拒绝、非法参数和跨租户路径。

审计证据：数据库结构和 Repository/API 已实现，但此前 TODO 所称的业务版本冲突和人工字段同步保护集成测试在仓库中不存在。

### T0904 加固 Excel/CSV 文件验收矩阵（P1）

- [ ] 增加真实复杂 `.xls` 样本，覆盖多工作表、日期、公式缓存值、空单元格和非 ASCII 内容。
- [ ] 为中文、空格、符号和数字开头的表头建立稳定安全字段标识与原始列名映射，使 DSL 不必直接把不合规源列名当作标识符。
- [ ] 增加 Excel/CSV Multipart HTTP 上传、重传、权限、配额、错误码和对象存储清理测试。
- [ ] 增加损坏 GBK/GB18030、非法方言、超长字段、超大文件和大文件内存基准。
- [ ] 明确 CSV 公式注入在预览、导出和再次生成 CSV 时的转义策略。

审计证据：当前 GBK、分号、Tab 和自定义引号已经通过读取器与 MinIO 同步集成测试，但复杂 `.xls`、HTTP 边界和恶意文件样本不足。

### T0905 完成生产级密钥与 Connector 通信安全（P1）

- [ ] 在 `EnvSecretResolver` 之外实现可替换的密钥管理 Provider、权限控制、缓存和轮换失效。
- [ ] 生产部署禁止使用默认内部令牌，支持令牌轮换，并评估 Connector 内网 TLS 或双向 TLS。
- [ ] 确保 Connector 错误、日志、追踪和审计中不出现密码、连接串或查询敏感样本。

审计证据：当前数据源只保存 `secret_ref`，满足不落明文的基础要求；但运行时只实现 `env://`，Go 与 Python 服务间使用静态内部令牌和明文 HTTP，尚未达到生产标准。

### T0906 建立 API 合同和完整 HTTP 回归（P1）

- [ ] 为认证、权限、数据源、文件和资产 API 建立 OpenAPI 或等价机器可校验合同。
- [ ] 增加真实 HTTP 层的认证、RBAC、请求大小、未知字段、并发冲突和安全错误响应测试。
- [ ] 在 CI 校验 API 合同兼容性，防止文档、Handler 和客户端字段漂移。

审计证据：当前已有 Markdown API 文档和部分 Handler 单元测试，但尚无统一机器可读合同；多项集成测试直接调用 Service/Repository，未覆盖完整中间件链路。

### T0907 提升自动化测试覆盖和质量门槛（P1）

- [ ] 为 `cmd`、数据库事务、对象存储、观测、错误处理和请求 ID 等当前低覆盖模块补测试。
- [ ] 为前端登录、刷新失败、权限路由、管理后台、设计器壳和查看器壳补测试。
- [ ] 在 CI 设定分阶段覆盖率基线，新增核心代码不得降低基线。
- [ ] 将 PostgreSQL 集成测试切换到每次运行可重建的一次性数据库/Schema，并让清理失败直接使测试失败；当前 `audit_logs` 的 append-only 触发器会阻止 `cleanupTenant` 删除已审计租户，而清理函数静默忽略了该错误，长期运行会残留测试租户和审计记录。

审计证据：本次 `go test -race -cover` 通过，但多个包覆盖率为 0%，数据源约 28.4%，资产约 5.2%；前端目前只有 1 个测试文件、3 个测试。T0206 集成测试写入审计后复核确认现有租户清理无法完整执行，需由隔离测试库方案收口。

### T0908 清理构建产物并改善仓库卫生（P2）

- [x] 修复 `scripts/ci-check.sh` 丢失的可执行位，并确认静态 CI 检查可直接运行。
- [ ] 确认 `bin/`、`web/dist/`、Python `__pycache__`、`.pytest_cache`、本地虚拟环境和 `.DS_Store` 不进入版本控制或交付制品清单。
- [ ] 增加仓库卫生检查，阻止缓存、二进制、真实凭证和超大临时文件进入提交。
- [ ] 对发布制品使用独立构建和校验流程，不依赖工作区已有产物。

审计证据：T0206 验证时发现 `scripts/ci-check.sh` 因缺少可执行位而直接运行失败，现已修复且检查通过。当前工作区仍存在上述本地产物；`.gitignore` 已覆盖其中多数，但当前目录不是可查询提交状态的 Git 工作树，因此无法确认历史提交是否干净。

### T0909 明确原型与正式实现边界（已完成）

- [x] 在代码和文档中标记 `internal/spike/federation`、`internal/spike/reportjson` 仅为技术原型；正式跨源运行时位于 `internal/federation`。
- [x] T0301/T0401 开始时重新设计版本化 Schema、迁移、错误路径、碰撞校验和兼容样本，不直接把原型视为正式实现。
- [x] 里程碑统计只按可运行业务链路计数，不以原型测试通过代替任务完成。

审计证据：原 `internal/spike/federation` Go Hash Join 仍仅用于基准和对照；正式跨源执行已通过 `internal/federation` 接入数据集运行时。原 `internal/spike/reportjson` 只保留合同哈希原型，正式版本化报告合同、迁移和校验位于 `internal/reportjson`，设计器、渲染器、发布和运行服务仍按后续任务实施。

### T0910 接入并验证模型配置（P2，主体完成，依赖 T0206/T0501 的真实模型验收）

- [x] 将 `AI_BASE_URL`、`AI_MODEL`、`AI_API_KEY`、`AI_REQUEST_TIMEOUT` 和 `AI_CONFIDENCE_THRESHOLD` 接入模型 Provider Adapter，禁止在日志、JSON 或数据库中落明文密钥。
- [x] 增加模拟 Provider 的超时、严格结构化输出和非法响应测试。
- [ ] 使用真实受控模型补充连通性、限额/限流、费用预算、服务端错误和 Schema 方言兼容测试。
- [x] 提供无模型密钥时的 `503 AI_PROVIDER_UNAVAILABLE` 明确降级状态，不影响非 AI 数据源和报告功能。

审计证据：`cmd/api/main.go` 已装配 `internal/metadataai` Provider、服务和 HTTP API；`.env.example` 提供非敏感配置项，密钥为空时 Provider 不发起请求。真实模型密钥与运营限额尚未进入自动化验收。
