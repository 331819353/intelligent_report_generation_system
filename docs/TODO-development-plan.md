# 智能化报告生成系统 TODO 开发计划

> 执行对象：Codex  
> 需求基线：`docs/intelligent-report-system-design-v1.0.md`  
> 核心业务后端：Go（Golang）；数据库连接层：独立 Python Connector Service  
> 当前状态：工程基座、租户权限、数据源与资产中心主体已落地；数据集及后续报告链路尚未开始  
> 最近审计：2026-07-15，已按代码、迁移、自动化测试和本地 Docker 实际运行结果复核  
> 计划原则：先打通可运行的纵向闭环，再扩展智能生成、跨源查询、调度和公开能力

---

## 1. Codex 执行规则

### 1.1 每次领取任务前

- 阅读本文件、需求文档及仓库中的 `AGENTS.md`（若后续新增）。
- 一次只领取一个可独立验收的任务；不得把多个阶段一次性混在同一变更中。
- 检查当前工作区状态，保留用户已有修改，不覆盖无关文件。
- 若任务依赖尚未完成，先完成依赖任务或明确报告阻塞，不用临时假实现掩盖依赖。
- 涉及数据库、对象存储、消息队列、联邦查询引擎或云模型时，先提供本地可替代实现和清晰接口边界。

### 1.2 每个任务必须包含

- 实现代码；
- 单元测试或集成测试；
- 必要的数据库迁移；
- API/Schema/配置文档更新；
- 本地验证命令；
- 对安全、租户隔离和版本兼容性的检查。

### 1.3 完成定义（Definition of Done）

任务只有同时满足以下条件才能标记为 `[x]`：

1. 需求和验收条件全部实现；
2. 格式化、静态检查、单元测试和相关集成测试通过；
3. 不包含明文凭证、临时调试代码或无法解释的跳过测试；
4. 新增配置均提供默认值和示例；
5. 新增 API 有请求、响应及错误码说明；
6. 新增持久化结构有向前迁移方案；
7. 多租户数据访问包含 `tenant_id` 校验；
8. Codex 在任务交付说明中列出修改文件、验证结果、遗留风险和下一建议任务。

### 1.4 建议统一验证命令

工程初始化后应提供统一入口，目标命令如下：

```bash
make fmt
make lint
make test
make test-integration
make build
```

前端初始化后增加：

```bash
make frontend-lint
make frontend-test
make frontend-build
```

---

## 2. 目标工程结构

以下结构由初始化任务创建，可在技术验证后微调：

```text
.
├── cmd/
│   ├── api/
│   ├── worker/
│   └── migrate/
├── internal/
│   ├── auth/
│   ├── tenant/
│   ├── datasource/
│   ├── metadata/
│   ├── dataset/
│   ├── metric/
│   ├── report/
│   ├── renderer/
│   ├── ai/
│   ├── scheduler/
│   ├── storage/
│   └── audit/
├── migrations/
├── api/
├── web/
├── configs/
├── deployments/
├── test/
├── docs/
├── Makefile
└── go.mod
```

---

## 3. 里程碑总览

| 里程碑 | 目标 | 退出条件 |
|---|---|---|
| M0 工程基座 | 建立可构建、可测试、可迁移的 Go/前端工程 | API、前端、PostgreSQL 本地启动且 CI 通过 |
| M1 租户与数据源 | 完成租户隔离以及 MySQL、Oracle、Excel 接入 | 三类数据源可创建、测试/解析和同步资产 |
| M2 数据集与指标 | 完成单源、跨源数据集和指标闭环 | 数据集可查询，指标可试算和版本化 |
| M3 报告设计器 MVP | 完成长画板、分块和组件编辑 | 配置可保存为 JSON 并由渲染器还原 |
| M4 AI 报告能力 | 完成整份报告生成和分块 AI 修改 | AI 变更可预览、校验、确认、撤销和审计 |
| M5 发布与运行 | 完成版本、权限、公开访问、缓存和容错 | 已发布报告可安全在线访问 |
| M6 调度与归档 | 完成定时运行、邮件、PDF 和历史归档 | 定时任务可生成并发送可追溯 PDF |
| M7 生产就绪 | 完成性能、安全、监控和灾备 | 核心验收项和上线检查全部通过 |

### 3.1 当前完成度审计（2026-07-15）

| 范围 | 审计结论 | 说明 |
|---|---|---|
| M0 工程基座 | 已完成 | Go、前端、CI、迁移和基础容器均可运行；Connector 本地测试依赖说明仍需回补 |
| M1 租户与数据源 | 主体完成，未达到完整退出条件 | 租户、认证、RBAC、MySQL、Oracle、Excel/CSV、资产中心和元数据 AI 手动补全链路已实现；公开策略、查询阶段行列权限、真实元数据同步/模型端到端验收及 AI 异步批量触发仍缺失 |
| M2 数据集与指标 | 未开始 | 只有行列策略编译器和 Go Hash Join 技术原型，不等同于数据集执行器 |
| M3 报告设计器 MVP | 未开始 | 只有报告 JSON 合同原型，未实现 Schema、设计器、长画板和渲染器 |
| M4 AI 报告能力 | 未开始 | 已提供环境变量示例，但尚无模型 Provider、AI 服务或分块 AI 浮窗 |
| M5 发布与运行 | 未开始 | 未实现报告版本、发布、公开访问、缓存和在线运行服务 |
| M6 调度与归档 | 未开始 | Worker 仅有工程入口，尚无任务、调度、邮件或 PDF 业务实现 |
| M7 生产就绪 | 未开始 | 已有基础日志和部分安全测试，但未形成监控、压测、灾备和完整安全矩阵 |

按 53 个编号任务统计：可认定完整完成 8 项（T0001-T0004、T0101-T0103、T0201）；部分完成或实现完成但验收待补 7 项（T0005、T0104、T0202-T0206）；其余 38 项尚未开始。该统计以本文件的完成定义为准，技术原型不计为业务任务完成。

---

## 4. P0：工程基座与架构验证

### T0001 初始化 Go 工程（已完成）

- [x] 创建 Go Module、`cmd/api`、`cmd/worker` 和内部模块目录。
- [x] 实现配置加载、结构化日志、错误码、请求 ID、优雅退出和健康检查。
- [x] 提供 `/health/live`、`/health/ready`。
- [x] 创建 `Makefile` 的 `fmt/lint/test/build` 命令。
- [x] 增加基础单元测试。

验收：空环境可构建 API 和 Worker；健康检查返回明确状态。

### T0002 初始化前端工程（已完成）

- [x] 创建 `web` 工程并确定 TypeScript、路由和测试框架；状态管理保持 React 局部状态，待跨页面业务状态出现后再引入专用库。
- [x] 建立登录页、管理后台壳、设计器壳和在线查看器壳。
- [x] 建立统一请求客户端、错误提示和主题变量。
- [x] 提供 lint、单元测试和生产构建命令。

验收：四类页面可路由访问，前端测试和构建通过。

### T0003 建立本地开发环境（已完成）

- [x] 提供 PostgreSQL、缓存、对象存储的本地容器编排。
- [x] 提供 `.env.example`，不得提交真实密钥。
- [x] 实现数据库迁移命令；测试数据初始化将在 T0101 基础租户模型落地后补充。
- [x] 编写本地启动、停止、重置和排障说明。

运行验收状态（2026-07-15）：PostgreSQL、Redis、MinIO 全部健康，三个 MinIO 存储桶创建成功；迁移与数据库验证通过。

验收：新开发者按照文档可在一个工作日内启动完整基础环境。

### T0004 建立 CI 基线（已完成）

- [x] 配置 Go 格式化、静态检查、测试和构建。
- [x] 配置前端 lint、测试和构建。
- [x] 配置迁移文件一致性检查、依赖漏洞和密钥扫描。
- [x] 禁止测试失败时继续发布制品。

验收：故意引入格式、测试或密钥问题时 CI 能正确阻止合并。

### T0005 技术验证：Oracle、Excel、联邦查询与页面渲染（部分完成）

- [x] 验证 Oracle 连接、超时、取消和许可证条件（数据库连接层已切换为 Python `oracledb` Thin 模式，不依赖 Oracle Client/OCI；Oracle Free 23.26.2 真实查询通过）。
- [ ] 验证 `.xlsx`、`.xls` 的工作表读取、公式值、日期和大文件内存占用（`.xlsx` 自动化完成，`.xls` 适配器完成但复杂样本待补）。
- [ ] 对比独立联邦引擎与 Go 查询网关方案，记录 MySQL/Oracle/Excel Join 的执行结果（Go Hash Join 基准完成，独立引擎和真实源待对照）。
- [ ] 验证统一报告 JSON 在设计器、在线查看和 PDF 渲染中的一致性（合同一致性完成，真实 PDF 视觉验证待导出 Worker）。
- [x] 输出 ADR，明确首期采用方案和不采用方案。

验收：形成可复现原型、压测数据、风险清单和架构决策记录。

---

## 5. P1：多租户、身份与权限

### T0101 建立控制库基础模型（已完成）

- [x] 创建租户、用户、角色、用户角色、权限、审计日志表。
- [x] 所有租户业务表统一包含 `tenant_id`，核心实体包含创建/更新时间和版本字段。
- [x] 为租户字段、状态字段和常用查询建立索引。
- [x] 建立只向前迁移和前向修复策略。

运行验收状态（2026-07-15）：迁移 SQL 已在 PostgreSQL 17 成功执行；`report_admin`/`report_app` 账号分离、双租户 RLS、跨租户关联阻断和审计不可变全部通过数据库验证。

验收：两个租户可创建同名资源且互不可见。

### T0102 实现身份认证（已完成）

- [x] 实现登录、退出、访问令牌、轮换刷新令牌和 bcrypt 密码安全策略。
- [x] 从服务端验证通过的身份上下文解析 `tenant_id`，禁止普通请求覆盖。
- [x] 实现禁用用户、Token Version、会话撤销和登录/退出审计。

验收：伪造租户 ID、过期令牌和禁用用户均不能访问业务 API。

### T0103 实现 RBAC 与对象权限（已完成）

- [x] 实现功能权限、对象权限和操作权限核心模型。
- [x] 提供平台管理员、租户管理员、数据管理员、分析师、报告设计师和查看者基础角色种子。
- [x] 将权限中间件接入权限管理 API，并补齐角色、授权、撤销管理 API 与数据库集成测试；后续业务模块必须复用同一中间件。

验收：角色矩阵自动化测试覆盖允许和拒绝路径。

### T0104 实现行级与列级权限框架（核心框架完成，查询链路回补中）

- [x] 定义行级策略 DSL 和列级隐藏、掩码、仅聚合策略。
- [ ] 从数据库加载适用策略并在真实查询编译阶段注入，不依赖前端过滤。
- [x] 缓存键包含租户、用户或权限版本。

验收：同一数据集对不同用户返回不同授权结果，缓存不串租户。

### 阶段回补清单

- [x] 建立真实 PostgreSQL RBAC、对象权限和策略集成测试。
- [x] 完成角色、角色权限、用户角色、对象授权与撤销 API。
- [x] 强制校验对象权限主体属于当前租户。
- [ ] 实现策略仓库、用户/角色适用范围匹配和查询执行接入（仓库与安全查询计划已完成，等待 T0301 数据集执行器接入）。
- [x] 执行 `AGGREGATE_ONLY` 最小分组规模约束。
- [x] 完成前端路由守卫、Bearer Token、自动刷新和服务端注销。
- [x] 建立 CI、迁移检查、依赖漏洞与密钥扫描。

### T0105 实现对象可见性与公开策略模型

- [ ] 支持 `PRIVATE`、`TENANT`、`PUBLIC`。
- [ ] 支持公开地址、访问码、有效期、禁止下载和撤销公开。
- [ ] 建立公开发布安全校验和访问审计。

验收：公开访问只能读取已发布版本，不能访问后台或敏感依赖。

---

## 6. P1：数据源与数据资产

### T0201 实现统一数据源接口

- [x] 定义 Go 领域层与 Python 数据连接服务之间的统一接口。
- [x] 实现创建、更新、启停、连接测试、同步和删除状态机（Go 领域层、PostgreSQL/RLS 持久化及 HTTP API 已接入）。
- [x] 敏感凭证只保存 `secret_ref` 或加密值。
- [x] 为每个租户定义连接池、并发和文件大小配额（数据库连接池和租户查询并发已强制执行；Excel 文件大小在 T0204 收口）。
- [x] 收口状态机和连接生命周期：草稿/错误状态不能绕过测试，删除时关闭 Python Pool，空闲 Pool 自动淘汰。

验收：新增数据源类型不需要修改上层数据集核心接口。

### T0202 实现 MySQL 数据源（功能完成，端到端验收待补）

- [x] 支持连接配置、连接测试和双层只读校验（失败关闭词法防护 + 源账号只读授权；完整 AST 重写归入 T0303/T0305）。
- [x] 同步 Catalog、表、视图、字段、注释、主外键和索引摘要。
- [x] 支持基于结构哈希的差异同步和失效标记。
- [ ] 使用真实 MySQL 完成“Go 数据源服务 → Python Connector 元数据同步 → PostgreSQL 快照/资产/差异”的端到端测试，并纳入 CI。

验收：满足需求文档 `DS-01`、`MD-01`、`MD-02`。

### T0203 实现 Oracle 数据源（功能完成，端到端验收待补）

- [x] 支持 Service Name/SID、Schema 范围和连接测试。
- [x] 同步表、视图、字段、注释、约束和索引摘要。
- [x] 处理 Oracle 空字符串、NUMBER、DATE 和大小写差异，并提供显式查询取消。
- [ ] 使用真实 Oracle 分别覆盖 Service Name、SID、Schema 范围、元数据持久化、超时和取消的端到端测试，并纳入 CI 或独立夜间流水线。

验收：满足需求文档 `DS-02`，连接取消和超时可控。

### T0204 实现 Excel 数据源（主体完成，样本与 HTTP 验收待补）

- [x] 支持上传 `.xlsx`、`.xls`、`.csv`，CSV 支持 UTF-8/GBK/GB18030、逗号/分号/Tab/单字符分隔符及自定义引号方言；在 MinIO 保存原始文件并记录 SHA-256。
- [x] 支持选择工作表、表头行、空行策略和列名去重规则。
- [x] 推断文本、整数、小数、布尔、日期和时间类型，支持人工覆盖。
- [x] 将工作表映射为表资产、列映射为字段资产。
- [x] 重新上传生成不可变文件版本并输出工作表/字段差异。
- [x] 为已发布数据集提供不可变文件版本 ID；数据集外键在 T0301 数据集模型中接入。
- [ ] 补充复杂 `.xls` 样本、Multipart HTTP 上传/重传、损坏 GBK/GB18030、超大 CSV 和公式注入安全测试。

验收：满足需求文档 `DS-03`；异常文件、超限文件和公式错误有明确提示。

### T0205 实现数据资产中心（功能完成，集成验收不足）

- [x] 实现表资产、字段资产、业务描述、标签、敏感级别和人工锁定。
- [x] 实现搜索、筛选、详情、版本差异和下游影响入口。
- [x] 实现租户内目录公开与只读复用规则。
- [ ] 补充资产业务元数据更新、业务版本冲突、人工锁定字段同步保护、目录公开、差异查询和影响分析的 PostgreSQL/API 集成测试；当前仅有跨租户读取隔离测试和模型校验测试。

验收：数据管理员可完成资产查找、补全、锁定和变更追踪。

### T0206 实现元数据 AI 补全（主体完成，真实模型与异步批量验收待补）

- [x] 定义 OpenAI-Compatible 模型供应商抽象层和严格结构化输出 Schema；本地校验为最终安全边界。
- [x] 对表字段生成业务名称、业务描述、标签、语义类型、敏感级别建议和置信度。
- [x] 低置信度、人工锁定和生成期间版本变化结果进入待确认；支持显式接受/拒绝且再次执行锁与版本检查。
- [x] 记录 Provider、模型、提示词版本、Token、输入哈希、延迟、结构化结果、状态和审计信息，不落 API Key、原始提示词或失败响应正文。
- [x] 增加非法/缺失/虚构资产 ID、非法标签与语义类型、超时、人工锁定、低置信度、跨租户隔离和建议决策测试；PostgreSQL 迁移与集成测试已通过。
- [ ] 使用真实受控模型完成连通性、Schema 兼容性、超时和提示词质量验收，并建立 Token/费用预算、租户限额与限流策略。
- [ ] 将当前“按表手动同步调用”扩展为元数据同步后的异步批量任务，补齐幂等、重试、取消和失败恢复，避免同步接口被模型延迟阻塞。
- [ ] 将内置基础标签枚举扩展为租户可配置标签字典，同时保持输出 Schema 白名单约束和历史结果兼容。

验收：模型输出非法、超时或虚构内容时不会污染正式资产。

验收证据：`internal/metadataai` 单元测试覆盖严格结构化输出、未知/缺失字段拒绝、超时失败和锁/版本/置信度分流；`test/integration/metadata_ai_test.go` 覆盖正式资产应用、待确认、人工决策、Token/审计记录与 RLS 隔离。当前未配置真实模型密钥，真实 Provider 质量和运营限额仍待验收。

---

## 7. P2：数据集、查询与指标

### T0301 定义数据集 DSL V1

- [ ] 定义节点、Join、字段、过滤、聚合、排序、参数和输出粒度。
- [ ] 提供 JSON Schema、版本迁移器和示例。
- [ ] 实现 DSL 保存、加载和确定性规范化。

验收：同一 DSL 可稳定生成相同规范结构和查询计划。

### T0302 实现单源数据集设计器

- [ ] 支持选择表、字段、Join、过滤、聚合、计算字段和参数。
- [ ] 支持小样本预览和错误定位。
- [ ] 保存 DSL，不把生成 SQL 作为唯一事实来源。

验收：MySQL、Oracle、Excel 均可创建单源数据集并重新加载编辑。

### T0303 实现安全查询编译器

- [ ] 实现参数绑定、只读校验、字段白名单和查询超时。
- [ ] 注入租户、行级和列级权限。
- [ ] 实现查询取消、结果行数限制和审计。

验收：DDL/DML、注释绕过、堆叠语句和越权字段测试全部失败关闭。

### T0304 实现跨源查询

- [ ] 支持 MySQL ↔ Oracle、数据库 ↔ Excel、Excel ↔ Excel Join。
- [ ] 实现规范类型转换、过滤下推和必要字段裁剪。
- [ ] 实现实时、缓存和物化三种执行模式。
- [ ] 保存执行计划、数据水位和来源版本。

验收：满足 `XD-01`、`XD-02`，结果可追溯到固定源版本。

### T0305 实现自定义 SQL

- [ ] 支持 MySQL/Oracle 源端只读 SQL。
- [ ] 支持联邦 SQL 或等价高级查询表达。
- [ ] 使用 AST 校验、参数绑定、权限重写和执行限额。

验收：自定义 SQL 不可绕过对象、行级和列级权限。

### T0306 实现指标与维度管理

- [ ] 实现指标表达式、聚合、单位、时间粒度和适用维度。
- [ ] 实现试算、草稿、发布、废弃和版本引用。
- [ ] 实现可加性、Join 扇出和引用循环校验。

验收：满足 `MET-01`，发布报告引用不可变指标版本。

### T0307 实现血缘与影响分析

- [ ] 建立源文件/源表 → 字段 → 数据集 → 指标 → 组件 → 报告版本血缘。
- [ ] 元数据变化后生成受影响对象清单。
- [ ] 为高风险变化阻止发布或要求确认。

验收：删除或改型字段时能定位所有受影响报告组件。

---

## 8. P3：报告 JSON、画板与组件

### T0401 定义报告 JSON Schema V1

- [ ] 覆盖报告、画布、页面、分块、组件、参数、样式、绑定、冻结和权限引用。
- [ ] 明确 1920×1080 是首屏基准，12×10 是首屏网格，纵向行数动态增长。
- [ ] 提供旧字段迁移规则、Schema 校验器和完整示例。

验收：有效示例全部通过，无效坐标、碰撞和未知组件能给出精确错误路径。

### T0402 实现统一页面渲染器

- [ ] 仅依赖版本化报告 JSON 和运行上下文还原页面。
- [ ] 设计器预览与在线查看共用渲染核心。
- [ ] 实现组件注册、未知组件降级和局部失败边界。

验收：相同 JSON 在设计器预览和查看器中的结构一致。

### T0403 实现长画板和主网格

- [ ] 固定 12 列、首屏 10 行，纵向内容行按分块最大 `y+h` 增长。
- [ ] 实现拖拽、缩放、吸附、对齐、碰撞和越界校验。
- [ ] 按容器宽度缩放，纵向自然滚动。

验收：分块可移动到第 10 行以后，刷新后坐标和高度不变。

### T0404 实现分块内动态网格

- [ ] `M×N` 分块生成 `4M×4N` 内网格。
- [ ] 支持在分块内拖入、移动、缩放、复制和删除组件。
- [ ] 分块缩放后重算内网格并处理组件越界。

验收：满足 `CAN-02`、`CAN-04`。

### T0405 实现分块清空、删除和 1×1 恢复

- [ ] 实现“清空分块”和“删除分块”两个操作。
- [ ] 操作后将原 `M×N` 区域恢复为 `M×N` 个隐式空白 `1×1` 基础单元。
- [ ] 非空操作二次确认并展示组件数量。
- [ ] 支持撤销/重做、锁定和权限校验。

验收：满足 `CAN-05`，画板不出现不可选择的空洞。

### T0406 实现组件库第一批

- [ ] 标题、富文本、筛选器、指标卡、附加信息卡。
- [ ] 表格、柱状图、条形图、折线图、饼图。
- [ ] 图片、附件、数据来源、更新时间和结论组件。
- [ ] 每类组件提供配置 Schema、默认值、最小尺寸和错误态。

验收：所有组件可从 JSON 创建、编辑、复制、删除和重新加载。

### T0407 实现筛选、联动和数据绑定

- [ ] 筛选器绑定报告参数和语义字段。
- [ ] 支持报告、页面、分块和指定组件作用域。
- [ ] 支持图表点击联动、下钻和参数刷新。

验收：跨数据集筛选通过语义字段映射正确生效。

### T0408 实现浏览态冻结

- [ ] 支持分块和组件的 sticky 开关、顶部偏移、作用域和层级。
- [ ] 多冻结项自动堆叠，离开约束容器后停止悬浮。
- [ ] PDF/打印模式恢复普通文档流。

验收：满足 `CAN-03`，滚动、缩放和多冻结项场景通过视觉测试。

### T0409 实现报告 JSON 持久化

- [ ] 草稿使用完整 JSON + JSON Patch 保存。
- [ ] 发布生成不可变 JSON 文件、哈希、Schema 版本和对象存储地址。
- [ ] 组件索引、血缘索引由 JSON 派生，不成为第二事实来源。

验收：仅凭发布 JSON 和授权运行上下文即可还原报告页面。

---

## 9. P4：AI 报告与分块 AI 浮窗

### T0501 实现 AI 编排基础设施

- [ ] 实现文本/视觉模型 Provider Adapter。
- [ ] 实现超时、重试、限额、成本统计和租户级授权。
- [ ] 实现输入脱敏、最小化和完整 AI 审计。

验收：供应商切换不影响报告领域接口；禁止发送凭证和未授权字段。

### T0502 实现两阶段报告生成

- [ ] 接收需求描述、附件、图片和授权资源范围。
- [ ] AI 先生成语义草稿 JSON。
- [ ] 服务端解析真实数据集、指标、字段和组件引用。
- [ ] 未解析资源明确标记，不允许静默执行。

验收：满足 `GEN-01`、`GEN-02`。

### T0503 实现分块 AI 浮窗交互

- [ ] 鼠标悬停或选中分块时显示 AI 入口；触屏使用选中模式。
- [ ] 支持自然语言输入、快捷指令、处理中、失败和取消状态。
- [ ] 浮窗不遮挡分块关键操作，键盘可访问。

验收：任意可编辑分块均可唤起，锁定或无权限分块不能发起修改。

### T0504 实现分块级 AI 上下文与 JSON Patch

- [ ] 默认仅发送当前分块 JSON、主题和授权语义资源。
- [ ] AI 只能返回当前分块路径内的 JSON Patch。
- [ ] 校验 Schema、权限、绑定、碰撞、尺寸和人工锁定状态。
- [ ] 展示变更前后预览，确认后应用，支持撤销和审计。
- [ ] 跨分块修改必须由用户明确启用并再次确认。

验收：满足 `CAN-06`，恶意或越界 Patch 被服务端拒绝。

### T0505 实现 AI 结论生成

- [ ] 结论只使用绑定指标、维度结果和数据快照。
- [ ] 保存证据引用、模型版本和生成时间。
- [ ] 支持人工修订、锁定和重新生成差异预览。

验收：结论中的数字均可追溯，人工锁定内容不会被覆盖。

---

## 10. P5：发布、在线运行与公开访问

### T0601 实现报告版本与发布校验

- [ ] 区分报告定义、不可变报告版本和报告运行实例。
- [ ] 发布前校验 JSON、依赖版本、权限、查询、附件、冻结和布局。
- [ ] 发布失败返回组件级错误路径。

验收：草稿修改不会改变已发布版本。

### T0602 实现报告运行服务

- [ ] 解析组件依赖并合并重复查询。
- [ ] 注入权限快照、参数和数据水位。
- [ ] 支持 `ONLINE/SCHEDULED/EXPORT/PREVIEW` 运行类型。
- [ ] 单组件失败不导致整页白屏。

验收：运行记录可追溯到报告、数据集、指标和 Excel 文件固定版本。

### T0603 实现缓存和物化

- [ ] 缓存键包含租户、权限版本、参数、数据集版本和数据水位。
- [ ] 支持查询缓存、物化数据集和整份报告快照。
- [ ] 实现失效、刷新、容量和租户配额策略。

验收：不存在跨租户或跨权限缓存污染。

### T0604 实现在线查看器

- [ ] 支持长页面、筛选、联动、下钻、刷新和冻结。
- [ ] 支持加载、空数据、部分失败和权限不足状态。
- [ ] 展示数据更新时间、来源和报告版本。

验收：满足 `RUN-01`，设计器预览与在线查看视觉一致。

### T0605 实现公开访问

- [ ] 生成不可猜测公开地址，支持访问码、有效期和禁止下载。
- [ ] 公开运行使用脱敏快照或公开专用权限身份。
- [ ] 撤销公开后立即失效地址和缓存。
- [ ] 增加限流、防枚举、安全响应头和访问审计。

验收：满足 `PUB-01`，匿名访问无法横向读取其他报告或后台资源。

---

## 11. P6：调度、邮件与 PDF

### T0701 实现持久化任务框架

- [ ] 实现任务创建、领取、心跳、重试、取消、死信和幂等键。
- [ ] 按租户限制并发和资源占用。
- [ ] 提供任务状态、错误和耗时查询。

验收：Worker 重启后任务不丢失、不重复产生不可逆副作用。

### T0702 实现报告调度

- [ ] 支持 Cron、时区、参数模板、启停和补跑。
- [ ] 调度固定引用报告版本或明确的“最新已发布版本”策略。
- [ ] 记录计划时间、实际时间和数据水位。

验收：满足 `SCH-01`，跨时区和错过执行窗口行为明确。

### T0703 实现 PDF 渲染与归档

- [ ] 使用受控浏览器加载同一报告 JSON 和运行快照。
- [ ] 处理长页面分页、图表完成信号、字体、页码和水印。
- [ ] 保存 PDF 哈希、对象地址、报告版本和数据快照引用。

验收：满足 `PDF-01`，相同版本和快照可复现一致 PDF。

### T0704 实现邮件发送

- [ ] 支持摘要、PDF 附件和受控链接。
- [ ] 发送前校验收件人授权和公开策略。
- [ ] 实现重试、退信记录和敏感信息保护。

验收：满足 `MAIL-01`，不同权限收件人不会共享错误附件。

### T0705 实现历史版本与归档中心

- [ ] 查看报告版本、运行参数、数据水位、结论修订和归档文件。
- [ ] 支持权限内下载、过期和保留策略。

验收：满足 `HIST-01`，任一归档可完整追溯生成依据。

---

## 12. P7：质量、安全与生产就绪

### T0801 完成自动化测试矩阵

- [ ] 单元测试覆盖核心领域规则和编译器。
- [ ] 集成测试覆盖 PostgreSQL、MySQL、Oracle、Excel、缓存和对象存储。
- [ ] 端到端测试覆盖登录、建模、设计、AI 修改、发布、公开、PDF 和邮件。
- [ ] 建立报告 JSON 与数据集 DSL 兼容性样本库。

### T0802 完成安全测试

- [ ] 租户越权、对象越权、行列权限和缓存污染测试。
- [ ] SQL 注入、文件上传、公式注入、路径穿越和恶意压缩文件测试。
- [ ] 公开链接枚举、访问码暴力破解和撤销失效测试。
- [ ] AI Prompt Injection、越权资源引用和越界 JSON Patch 测试。

### T0803 完成性能压测

- [ ] 测试已缓存报告首屏 P95。
- [ ] 测试长画板、大量组件和多冻结项渲染。
- [ ] 测试万级数据、跨源 Join、Excel 大文件和并发导出。
- [ ] 输出容量模型和租户默认配额。

验收：缓存/物化命中时已发布报告首屏 P95 ≤ 1 秒，或记录经过批准的例外。

### T0804 建立可观测性

- [ ] 统一日志、指标和追踪关联请求、查询、AI、任务和报告运行。
- [ ] 建立错误率、延迟、连接池、缓存、任务积压、模型成本和组件失败率告警。
- [ ] 日志自动脱敏，不输出连接串、密码、Token 和敏感数据样本。

### T0805 完成备份、恢复与发布方案

- [ ] 备份 PostgreSQL、对象存储 JSON/PDF/Excel、迁移记录和密钥引用元数据。
- [ ] 演练恢复、回滚和 Schema/JSON 版本迁移。
- [ ] 编写部署、升级、降级和故障处理手册。

### T0806 执行最终验收

- [ ] 逐项执行需求文档第 29 章验收标准。
- [ ] 对未通过项建立缺陷、责任任务和目标版本。
- [ ] 输出上线检查表、已知限制和后续迭代清单。

---

## 13. 首个可运行纵向切片

Codex 应优先完成以下最小链路，避免各模块长期处于不可集成状态：

1. `T0001` Go 工程；
2. `T0002` 前端工程；
3. `T0003` PostgreSQL/对象存储本地环境；
4. `T0101` 基础租户模型；
5. `T0204` Excel 数据源；
6. `T0301` 数据集 DSL；
7. `T0302` Excel 单源数据集；
8. `T0401` 报告 JSON Schema；
9. `T0402` 页面渲染器；
10. `T0403` 长画板；
11. `T0405` 分块清空/删除；
12. `T0406` 标题、表格、柱状图和结论四类组件；
13. `T0409` JSON 文件发布；
14. `T0601` 报告版本；
15. `T0604` 在线查看器。

该纵向切片完成后，用户应能：上传 Excel → 创建数据集 → 配置指标/图表 → 编辑长画板 → 清空或删除分块 → 保存报告 JSON → 发布 → 在线查看。

---

## 14. 暂不进入首个纵向切片的事项

以下能力保留在总体计划中，但不得阻塞首个可运行版本：

- 亿级明细实时分析；
- 多人实时协同编辑；
- 复杂 ETL 和数据仓库开发；
- 专业出版级自由排版；
- 大量高级图表和地图能力；
- 多模型自动路由优化；
- 除 MySQL、Oracle、Excel 之外的数据源；
- 移动端专用设计器。

---

## 15. Codex 任务交付模板

每完成一个任务，Codex 应在最终回复中使用以下结构：

```text
任务：Txxxx <任务名称>
状态：完成 / 部分完成 / 阻塞

完成内容：
- ...

主要文件：
- ...

验证结果：
- 命令：...
- 结果：...

数据库/API/配置变化：
- ...

遗留风险：
- ...

建议下一任务：
- Txxxx ...
```

> TODO 勾选必须与真实完成状态一致。未经测试或只创建空接口的任务不得标记为完成。

---

## 16. 2026-07-15 全链路审计新增回补任务

以下任务来自本次对需求、TODO、代码、迁移、测试和 Docker 运行状态的逐项复核。它们用于补足原计划中描述过于笼统或完成状态高于证据的问题，不替代 T0301 之后的主线开发。

### T0901 修复 Connector 本地开发与测试入口（P0）

- [ ] 在开发文档中加入 Python 3.13 虚拟环境和 `requirements-dev.txt` 安装步骤。
- [ ] 让 `make connector-test` 明确使用可配置的 Python 解释器或项目虚拟环境，并在缺少依赖时给出可操作提示。
- [ ] 在全新环境验证 Connector 的 15 项测试可由文档命令一次通过。

审计证据：CI 会先安装 Python 依赖，因此流水线定义可用；当前本机直接运行 `make connector-test` 因缺少 `oracledb` 收集失败，使用包含开发依赖的容器运行则 15 项通过。

### T0902 补齐真实数据库元数据端到端验收（P0）

- [ ] MySQL 覆盖连接测试、元数据同步、结构变化、删除失效、重新激活和只读查询。
- [ ] Oracle 覆盖 Service Name、SID、Schema 过滤、元数据同步、类型映射、超时和取消。
- [ ] 测试必须经过 Go `Service` 和 Python Connector，并断言 PostgreSQL 中的快照、资产、结构哈希和差异记录。
- [ ] 将真实源测试加入 CI 的独立 Job；若 Oracle 启动时间不适合每次 PR，则至少建立可见的夜间流水线。

审计证据：现有 `test/sourceintegration` 验证了 MySQL/Oracle 查询和 Go Hash Join，但没有验证元数据同步落库及差异生命周期；当前 CI 也只启动 PostgreSQL、Redis、MinIO。

### T0903 补齐数据资产中心集成测试（P0）

- [ ] 覆盖表/字段业务元数据更新、乐观锁冲突和审计日志。
- [ ] 验证技术元数据刷新不会覆盖人工业务描述、标签、敏感级别、语义类型和锁定状态。
- [ ] 覆盖租户目录公开、搜索筛选、差异列表和下游影响分析。
- [ ] 覆盖资产管理 HTTP API 的允许、拒绝、非法参数和跨租户路径。

审计证据：数据库结构和 Repository/API 已实现，但此前 TODO 所称的业务版本冲突和人工字段同步保护集成测试在仓库中不存在。

### T0904 加固 Excel/CSV 文件验收矩阵（P1）

- [ ] 增加真实复杂 `.xls` 样本，覆盖多工作表、日期、公式缓存值、空单元格和非 ASCII 内容。
- [ ] 增加 Excel/CSV Multipart HTTP 上传、重传、权限、配额、错误码和对象存储清理测试。
- [ ] 增加损坏 GBK/GB18030、非法方言、超长字段、超大文件和大文件内存基准。
- [ ] 明确 CSV 公式注入在预览、导出和再次生成 CSV 时的转义策略。

审计证据：当前 GBK、分号、Tab 和自定义引号已经通过读取器与 MinIO 同步集成测试，但复杂 `.xls`、HTTP 边界和恶意文件样本不足。

### T0905 完成生产级密钥与 Connector 通信安全（P1）

- [ ] 在 `EnvSecretResolver` 之外实现可替换的密钥管理 Provider、权限控制、缓存和轮换失效。
- [ ] 生产部署禁止使用默认内部令牌，支持令牌轮换，并评估 Connector 内网 TLS 或双向 TLS。
- [ ] 确保 Connector 错误、日志、追踪和审计中不出现密码、连接串或查询敏感样本。

审计证据：当前数据源只保存 `secret_ref`，满足不落明文的基础要求；但运行时只实现 `env://`，Go 与 Python 服务间使用静态内部令牌和明文 HTTP，尚未达到生产标准。

### T0906 建立 API 合同和完整 HTTP 回归（P1）

- [ ] 为认证、权限、数据源、文件和资产 API 建立 OpenAPI 或等价机器可校验合同。
- [ ] 增加真实 HTTP 层的认证、RBAC、请求大小、未知字段、并发冲突和安全错误响应测试。
- [ ] 在 CI 校验 API 合同兼容性，防止文档、Handler 和客户端字段漂移。

审计证据：当前已有 Markdown API 文档和部分 Handler 单元测试，但尚无统一机器可读合同；多项集成测试直接调用 Service/Repository，未覆盖完整中间件链路。

### T0907 提升自动化测试覆盖和质量门槛（P1）

- [ ] 为 `cmd`、数据库事务、对象存储、观测、错误处理和请求 ID 等当前低覆盖模块补测试。
- [ ] 为前端登录、刷新失败、权限路由、管理后台、设计器壳和查看器壳补测试。
- [ ] 在 CI 设定分阶段覆盖率基线，新增核心代码不得降低基线。
- [ ] 将 PostgreSQL 集成测试切换到每次运行可重建的一次性数据库/Schema，并让清理失败直接使测试失败；当前 `audit_logs` 的 append-only 触发器会阻止 `cleanupTenant` 删除已审计租户，而清理函数静默忽略了该错误，长期运行会残留测试租户和审计记录。

审计证据：本次 `go test -race -cover` 通过，但多个包覆盖率为 0%，数据源约 28.4%，资产约 5.2%；前端目前只有 1 个测试文件、3 个测试。T0206 集成测试写入审计后复核确认现有租户清理无法完整执行，需由隔离测试库方案收口。

### T0908 清理构建产物并改善仓库卫生（P2）

- [x] 修复 `scripts/ci-check.sh` 丢失的可执行位，并确认静态 CI 检查可直接运行。
- [ ] 确认 `bin/`、`web/dist/`、Python `__pycache__`、`.pytest_cache`、本地虚拟环境和 `.DS_Store` 不进入版本控制或交付制品清单。
- [ ] 增加仓库卫生检查，阻止缓存、二进制、真实凭证和超大临时文件进入提交。
- [ ] 对发布制品使用独立构建和校验流程，不依赖工作区已有产物。

审计证据：T0206 验证时发现 `scripts/ci-check.sh` 因缺少可执行位而直接运行失败，现已修复且检查通过。当前工作区仍存在上述本地产物；`.gitignore` 已覆盖其中多数，但当前目录不是可查询提交状态的 Git 工作树，因此无法确认历史提交是否干净。

### T0909 明确原型与正式实现边界（P1）

- [ ] 在代码和文档中标记 `internal/spike/federation`、`internal/spike/reportjson` 仅为技术原型。
- [ ] T0301/T0401 开始时重新设计版本化 Schema、迁移、错误路径、碰撞校验和兼容样本，不直接把原型视为正式实现。
- [ ] 里程碑统计只按可运行业务链路计数，不以原型测试通过代替任务完成。

审计证据：Go Hash Join 和报告 JSON 合同原型测试覆盖良好，但尚未接入数据集、设计器、渲染器、发布或运行服务。

### T0910 接入并验证模型配置（P2，主体完成，依赖 T0206/T0501 的真实模型验收）

- [x] 将 `AI_BASE_URL`、`AI_MODEL`、`AI_API_KEY`、`AI_REQUEST_TIMEOUT` 和 `AI_CONFIDENCE_THRESHOLD` 接入模型 Provider Adapter，禁止在日志、JSON 或数据库中落明文密钥。
- [x] 增加模拟 Provider 的超时、严格结构化输出和非法响应测试。
- [ ] 使用真实受控模型补充连通性、限额/限流、费用预算、服务端错误和 Schema 方言兼容测试。
- [x] 提供无模型密钥时的 `503 AI_PROVIDER_UNAVAILABLE` 明确降级状态，不影响非 AI 数据源和报告功能。

审计证据：`cmd/api/main.go` 已装配 `internal/metadataai` Provider、服务和 HTTP API；`.env.example` 提供非敏感配置项，密钥为空时 Provider 不发起请求。真实模型密钥与运营限额尚未进入自动化验收。
