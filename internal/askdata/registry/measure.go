package registry

// Measure additivity shares the metric validation and certification contract
// in metric.go. This file intentionally marks the measure-version boundary;
// the current registry table is named askdata.measures but every row is an
// immutable version identified by VersionIdentity.
